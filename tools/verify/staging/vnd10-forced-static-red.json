{
 "name": "vnd10-forced-static-red",
 "why": "VND583-10 live check: skip the AOB winner's Aura::Init so Count==0 -- the recovery's own trigger -- and Genau::FindGObjectsStaticStruct runs on DumperTest's real static FUObjectArray (5.4: the 5.0-5.7 geometry; 5.8: ObjObjects first). RED: the same, built WITHOUT 70190f76's dll/src change.",
 "reverse_commit": "70190f76",
 "subs": [
  {
   "file": "dll/src/Frieren.cpp",
   "old": "    if (ptrs.GObjects) {\n        Aura::Init(ptrs.GObjects);\n    }\n",
   "new": "    if (ptrs.GObjects) {\n        // STAGING (stage_dll.py vnd10-forced-static): the AOB winner is not initialized, so Count==0 and the static recovery runs\n        LOG_WARN(\"UE5_Init: STAGING vnd10-forced-static: Aura::Init(0x%llX) skipped\", static_cast<unsigned long long>(ptrs.GObjects));\n    }\n",
   "count": 1
  }
 ]
}