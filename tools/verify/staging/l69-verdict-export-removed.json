{
 "name": "l69-verdict-export-removed",
 "why": "L69 step 2 RED arm: the same forced elemsize give-up as l69-l85-unmeasured-elemsize, but UE5_GetOffsetsVerdict is no longer EXPORTED -- the pre-3373056c C ABI, which carried no verdict. The function itself stays (Mimic's CMD 16 calls it), so only CE's symbol lookup loses it; HEAD's dissect must then build the structure silently.",
 "subs": [
  {
   "file": "dll/src/Genau.cpp",
   "old": "    const bool allMeasured = (unmeasured == 0);\n",
   "new": "    unmeasured |= 0x08;   // STAGING (stage_dll.py l69-verdict-export-removed): force the elemsize give-up\n    const bool allMeasured = (unmeasured == 0);\n",
   "count": 1
  },
  {
   "file": "dll/src/Frieren.h",
   "old": "__declspec(dllexport) int32_t  UE5_GetOffsetsVerdict(char* reasonBuf, int32_t bufLen);\n",
   "new": "/* STAGING (stage_dll.py l69-verdict-export-removed): NOT exported, as before 3373056c */ int32_t  UE5_GetOffsetsVerdict(char* reasonBuf, int32_t bufLen);\n",
   "count": 1
  }
 ]
}
