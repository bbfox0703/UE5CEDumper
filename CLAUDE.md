# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

> For detailed specs, implementation history, and debugging notes, see the **[docs/](docs/)** directory.

-----

## Build & Deploy
- After making code changes, always do a full rebuild and verify the build output is actually updated before testing. Never assume a build succeeded without checking.
- **Hand over an AOT-TRIMMED build, not the plain one.** `build.ps1` with no `-Mode` produces a
  self-contained **non-trimmed** exe (~107 MB); `-Mode Publish` produces the Native-AOT **trimmed**
  binary that ships (~54 MB). They are not the same program: reflection-shaped code — JSON without a
  source-generated context, MVVM bindings, `ComboBox.SelectedItem` bound to a boxed value, the
  reflection-based DataGrid column sort — compiles and runs fine untrimmed and fails **only** after
  trimming. **Every AOT bug in this repo's history was found by the maintainer re-compiling with AOT
  after being handed a non-trimmed build**, which costs a round trip every time.
  Use plain `build.ps1` / `-Target DLL` / `-Target Test` for fast iteration, then run
  `-Mode Publish` **before saying a UI change is ready to test**. It enters the VS DevShell itself
  (Native AOT links with MSVC's `link.exe`; without it ILCompiler dies with `exited with code 9009`,
  which reads like a broken toolchain rather than a missing environment).

-----

## Code Changes
- When asked to refactor or rename modules/files, make actual code changes (move files, update imports, rename classes) — not just documentation updates. Confirm structural changes before proceeding to docs.

-----

## Debugging
- When fixing bugs, verify the fix against the actual memory layout or data structure rather than assuming. If the first fix doesn't work, re-examine fundamental assumptions about the data format before iterating.

-----

## Git Operations
- When creating PRs, check for branch divergence and resolve merge conflicts before attempting `gh pr create`. Run `git status` and `git log --oneline -5` first.

-----

## Cheat Engine
- When working with CE Lua APIs, verify that functions/methods actually exist in the Cheat Engine Lua API before using them. Do not invent API calls.

-----

## Build & Dev Commands

### Unified Build Script (PREFERRED — always use this)

The `build.ps1` script handles VS DevShell setup, CMake, dotnet, and test execution automatically. **Always use this for building** — bare `cmake` / `dotnet build` commands will fail without the VS DevShell environment.

```bash
# Build everything (DLL + UI + Tests) — Release
powershell -NoProfile -ExecutionPolicy Bypass -File "D:\Github\UE5CEDumper\build.ps1"

# Build DLL + all 4 proxy DLLs (version/dinput8/dxgi/winmm — the injected artifacts)
powershell -NoProfile -ExecutionPolicy Bypass -File "D:\Github\UE5CEDumper\build.ps1" -Target DLL

# Build UI only
powershell -NoProfile -ExecutionPolicy Bypass -File "D:\Github\UE5CEDumper\build.ps1" -Target UI

# Run tests only
# ⚠ -Target Test does NOT compile the DLL. It builds only the two test executables
#   (`--target utf8_helpers_test` / `dll_helpers_test`), which link HEADERS — so a syntax
#   error in Fern.cpp / Stark.cpp / any other .cpp passes it clean. A green -Target Test
#   after editing a .cpp measures nothing about that file. Use -Target DLL (or no -Target)
#   before claiming a C++ change builds. Learned the hard way 2026-08-04: "959 dll green"
#   was reported over a Fern.cpp that had never been compiled and did not parse.
powershell -NoProfile -ExecutionPolicy Bypass -File "D:\Github\UE5CEDumper\build.ps1" -Target Test

# Debug build
powershell -NoProfile -ExecutionPolicy Bypass -File "D:\Github\UE5CEDumper\build.ps1" -Mode Debug

# Clean + publish (distribution)
powershell -NoProfile -ExecutionPolicy Bypass -File "D:\Github\UE5CEDumper\build.ps1" -Mode Publish -Clean
```

> **Why not bare cmake/dotnet?** The C++ DLL requires MSVC x64 environment (include paths, linker). `build.ps1` loads this via `Enter-VsDevShell` automatically. Running `cmake --build` without it causes `fatal error C1083: No such file or directory` for standard headers.

### Manual Commands (reference only — prefer build.ps1)

```bash
# C++ DLL (requires VS DevShell loaded first)
cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release
cmake --build build --config Release

# C# UI
dotnet build "ui/UE5DumpUI/UE5DumpUI.csproj" -c Release
dotnet test "ui/UE5DumpUI.Tests/UE5DumpUI.Tests.csproj"
```

### Git Submodules

```bash
git submodule update --init --recursive
```

-----

## Rules (MUST follow)

- **Language**: Code comments and UI strings in English
- **Single Instance**: UI app uses Mutex to ensure only one instance runs
- **Async everywhere**: All I/O, pipe operations, and alert actions must be async
- **Platform Abstraction**: Any system/OS-dependent call (P/Invoke, Registry, OS commands) MUST go through an interface in `Core` project. `Core` must NEVER contain direct platform-specific code
- **Log output**: All logs go to `%LOCALAPPDATA%\UE5CEDumper\Logs`, split by category into per-process subfolders. DLL: 5 files (init, scan, offsets, pipe, walk). UI: 3 files (init, pipe, view). Root `Logs/` has no loose files — only subfolders. **Retention is by AGE, not generation count**: `<cat>-0.log` is always the current run, the previous run is archived to `<cat>-YYYYMMDD-HHMMSS.log` stamped from its *own* mtime, and archives + whole per-process folders older than 21 days (`Grimoire::LOG_RETENTION_DAYS` / `Constants.LogMaxAgeDays`) are deleted at startup. 8 MB size cap per file still rotates mid-session. A file count could not express this — rotation runs on every process start, so N launches evicted everything earlier regardless of date
- **App-data layout**: `%LOCALAPPDATA%\UE5CEDumper` root holds only files that are **app-wide and fixed in number** (`dll-path.txt`, `ui-options.json`, `experimental.json`, `teleport-hotkeys.txt`, `window-state.txt`, `UE5CEDumper.{Machine}.json`). Anything **per-game** — one file per PE hash, so one more set on every game patch, forever — goes in its own PascalCase subfolder beside `Logs\` / `Reports\`: `Snapshots\` (`snapshots.<hash>.db` + `-wal`/`-shm`/`.denylist.json`), `Bookmarks\` (`bookmarks.<hash>.json`). A new per-game family MUST add a subfolder, not a root file. Folder creation, the one-time move of files left at the old root, and the age sweep are all `Services/AppDataFolderMaintenance.Prepare`, called **from the store's constructor** so nothing can read the folder before it is migrated (a composition-root call site is one reorder away from silently reading an empty folder). Three invariants: a game's files **move and expire as a GROUP** (a `.db` migrated without its `-wal` has silently dropped every transaction the WAL held — a blocked move rolls the whole group back); **"unused" = `LastWriteTimeUtc`, STAMPED by the store on use** — never last-access, whose NTFS auto-updates are ON by default (`fsutil behavior query DisableLastAccess` = 2), so AV/backup/indexer reads keep every file looking like today and the sweep silently never fires; and **retention is the STORE's call, not the folder's** — `Snapshots\` passes `Constants.DataMaxAgeDays` (21), `Bookmarks\` passes `0` (sweep off, deliberately: hand-placed navigation nobody can regenerate ≠ a regenerable multi-GB capture; see docs/todo.md, do not "finish" it by enabling the sweep)
- **Magic words management**: All magic strings kept in one file per project with proper comments
- **UI Strings**: English only. All UI strings in `Resources/Strings/en.axaml`, referenced via `StaticResource` bindings
- **Keyword search boxes (space = AND + per-keyword memory)**: Every client-side keyword/filter box in the UI MUST behave identically:
  - **space = AND**: whitespace-separated terms are combined with **AND** (term-level AND, field-level OR) via `Helpers/ObjectTreeFilter.MatchesAllTerms(string[] terms, params string?[] fields)` — never a single `.Contains`/`IndexOf` over one concatenated string. Split raw input with `ObjectTreeFilter.SplitTerms`. The 3-string overload is the allocation-free Object-Tree hot path (486K nodes); every other panel uses the `params` form. Server-side matchers that can't AND client-side (ValueSearch, SPC query-time) are the only exemption.
  - **per-keyword memory**: remember keywords the user actually typed *and that produced matches* via `Helpers/KeywordSearchMemory` (LRU `History` bound to an `AutoCompleteBox.ItemsSource`; `AutoCompleteBox` uses `PlaceholderText` not `Watermark`, and `Text` must be `Mode=TwoWay`). VM wiring = field + `XxxHistory` property + ctor probe `() => (FilterText, Results.Count > 0)` + `_mem.Schedule(value)` in the `OnXxxChanged` handler; `Flush()` before clearing the box on tab-switch/navigation.
  - **async/server-side count**: filters whose match count only lands after a pipe round-trip call `KeywordSearchMemory.Commit()` from the reload-completion path, NOT `Schedule` — a debounce probe races the async reload and reads a stale count.
- **UE offsets**: All UObject/UStruct offsets must be dynamically verified via OffsetFinder, never hardcoded
- **AOT compatible**: All C# code must be Native AOT / trimming compatible. No reflection-based APIs — use source generators instead (e.g. `[JsonSerializable]` context for `System.Text.Json`, `[ObservableProperty]` for MVVM). The UI is published as a self-contained trimmed binary
- **Module naming (Frieren convention)**: Every new C++ DLL module (a file with its own namespace) MUST take an unused character name from the **Frieren roster** in [docs/naming-convention.md](docs/naming-convention.md) — never a plain-English name (the historical plain-English modules have all been migrated: `Cancel`→`Tot`, `PackedItem`→`Lineal`, `ValueScan`→`Radar`). Add the required header comment (`// {Name} — {中文名} ({meaning})` + a one-line function description) and flip that name's status to 🟢 in use in the roster. Kept English by design (the only exceptions): generic leaf utilities (`Utf8Helpers.h`) and algorithm helpers that live inside an existing namespace (`GraphPath.h` under `Aura::`)
- **CE Lua output hygiene**: Every CE Lua script we emit — the C# script generators (`*ScriptGenerator.cs`, `CeXmlExportService`), the standalone `scripts/*.lua`, and `scripts/UE5CEDumper.CT` — MUST be quiet by default so the CE Lua Engine window never covers Cheat Engine.
  - **DEBUG flag**: gate every diagnostic/progress `print()` behind a flag. Global master switch with per-script override: `local DEBUG = UE5_DEBUG or 0` (unset = off; set `UE5_DEBUG=1` in CE's Lua console to make everything verbose). Print via a `dbg()` wrapper: `local function dbg(...) if DEBUG ~= 0 then print(...) end end`. Standalone `.lua`/`.CT` gate inline on `(UE5_DEBUG or 0) ~= 0`.
  - **Errors always surface**: real failures keep bare `print()` / `showMessage()` (ungated), and warnings that flag a genuine problem (e.g. dissect `warn`, `ue5_logError`) stay unconditional.
  - **Auto-close on clean success**: when `DEBUG == 0` and the block finished with no error, close the window via `synchronize(function() getLuaEngine().Close() end)`. On ANY error path (mailbox-not-found, timeout, `state<0` / `code==-7` / `result~=0`, null mid-walk, AOB-scan-fail) the close MUST be unreachable — `return` before it or guard it so the window stays open. A timeout is an error, so `return` (not `break`) out of a mailbox-wait loop that precedes a close.
  - **Reuse the shared emitter**: generators call `CeLuaHygiene.AppendDebugPreamble` / `AppendCloseOnSuccess` / `AppendMailboxWait` / `AppendContractCheck` / `AppendIdleWait` (and `CeLuaHygiene.CloseCall`) so the emitted text is byte-identical everywhere and can't drift. Each `{$lua}` block is its own Lua chunk — locals don't cross `[ENABLE]`/`[DISABLE]`, so emit the preamble in every block that uses `DEBUG`/`dbg`. **Hand-rolling any of these is how build 2743's three defects reached all seven copies of the mailbox wait at once.**
  - **A bail-out that applied NOTHING must untick the record** (`if memrec then memrec.Active = false end`) before returning — otherwise CE leaves the row ticked and the user is told a cheat is active when nothing was set. Two script shapes, and they are **not interchangeable**: *stateful toggles* (Movement / GodMode / Fly / SeeThrough / Foreground / DebugCamera / TimeDilation) untick-and-return, while *momentary actions* (Teleport's rows) flag `hadError` and leave the loop so their **deferred** untick timer still runs — an early `return` there skips it. Pass the right `MailboxTimeout` mode; `CeMailboxBailoutTests` asserts both shapes.
  - **Never report a mailbox failure by guessing.** `status` already distinguishes them: `0` = the DLL never picked the command up (usually a **stale** `g_invokeMailbox` address), `0xFF` = it took the command and wedged. And a timeout must be a REAL deadline via `getTickCount()` — `sleep(1)` measures **15.47 ms** in CE (the ~64 Hz Windows tick, identical on very different CPUs), so counting iterations against a millisecond constant made a "10 s" timeout ~155 s.
- **CE Lua ↔ DLL contract version**: versioned on the **CONTRACT**, never the build number — a `.CT` saved months ago stays valid against a newer DLL as long as nothing it depends on moved. `Mimic::MAILBOX_CONTRACT` + `MAILBOX_CONTRACT_MIN` publish a **range** via the exported `g_mailboxContract`; scripts bake `CeMailboxLayout.ContractVersion` and check `MIN ≤ script ≤ CONTRACT` **before the first write** (the layout is what is in question, so a write can't come first). Bump rules and what counts as the contract live in [`dll/src/Mimic.h`](dll/src/Mimic.h) — additive changes (new `Cmd`, new op at an unused number, new knobId) move `MAILBOX_CONTRACT` only; breaking ones also move `MAILBOX_CONTRACT_MIN`. `tools/check_mailbox_contract.py` hashes the surface and fails CI on a forgotten bump, which is **worse than no versioning** (every old script then asserts compatibility while writing to moved offsets). ⚠ **The hash covers field LAYOUT, not field MEANING** — a command that starts reading or writing a field it never touched before is a real contract change the hash cannot see (that is how contract 2 moved without the hash moving). Its "bumped but surface unchanged" branch is what surfaces those: it refuses the bump until you record WHY in the golden block.

-----

## Project Goal

Develop a **C++ DLL + Cheat Engine Lua Bridge** Unreal Engine Dumper.
Target: Support UE4 (4.18+) and UE5 (5.0 ~ 5.8+), find global pointers (GObjects / GNames / GWorld), build complete object/struct hierarchy, integrate with CE. Despite the "UE5" name, UE4 games are a priority target — many popular games use UE4, and RE-UE4SS demonstrates broad UE4 support is achievable.

-----

## Architecture Overview

```
+------------------------------------------------------+
|  Game Process                                        |
|                                                      |
|  CE Lua                                              |
|    UE5CEDumper.CT     <- DLL inject + pipe start     |
|    ue5_dissect.lua    <- CE Structure Dissect builder |
|    ue5_invoke_helper.lua <- AA Script invoke shim (embedded in CT via UI) |
|      |  loadLibrary / callFunction (59 C ABI exports)|
|      v                                               |
|  UE5Dumper.dll  [injected]                           |
|      +-- Frieren  (ExportAPI)     <- C ABI for CE Lua (13 teleport incl. Relative/RecallExplicit/Set+GetMouseCursor, build 1144) |
|      +-- Fern     (PipeServer)    <- Named Pipe IPC (~87 cmds incl. value scan + group scan + teleport + teleport_relative + set/get_mouse_cursor + get_related_objects + get_current_target + get/set_movement_multiplier/reset_movement (Laufen) + set/get_foreground_lock (Grausam) + seethrough_set/get_state (Schlacht) + pe_profile_start/stop/get (Linie) + force_field/reset_field/reset_all_fields/get_forced_fields/find_stealth_meter (Solide); find_path_from_gworld deep/container_depth + teleport_get_pose loc/vel field addrs, ~92 cmds, 2168 — the live count is **99**, derive with `grep -c 'constexpr const char* CMD' dll/src/Renge.h`; + query_group_slot_leaves (name every leaf one group slot kept — the row can only display one, 2715)) |
|      +-- Himmel   (Signatures)    <- 151 AOB + 1 CallFollow + 6 symbol exports = **158 entries** over 5 targets (GObjects/GNames/GWorld/SparseDelegates/GEngine), 31 source tags; tables static_assert-sorted by priority, no dead constants (extract_patterns.py checks, `--check` asserts these counts in CI); scripted 57-row / 70-program sweep (38 full-PDB oracles) across UE 4.10-5.8 via tools/ghidra/sweep.sh. **Counts here are DERIVED — regenerate with `py tools/ghidra/extract_patterns.py dll/src/Himmel.h out.tsv`, never hand-edit**, 2530 |
|      +-- Genau    (OffsetFinder)  <- GObjects/GNames/GWorld + **&GEngine slot** (FindGEngineSlot, AOB; cross-version UE4.27+UE5.7, 2399) + FindGameEngine (live UEngine, O(1) off the slot when resolved); CE Lua get GWorld (&slot+deref) / GameEngine addr via Mimic CMD_QUERY_PTR=13 mailbox, 1978; + **pre-UE4 refusal** (CountPreUE4Markers -> Grimoire::PRE_UE4_SENTINEL_VERSION=300 -> the existing too-old gate; 2-of-4 POSITIVE UE3 markers, checked only in DetectVersionDetailed's terminal branch so "no UE4/UE5 evidence" is structural, 2508) |
|      +-- Aura     (ObjectArray)   <- Chunked + Flat arrays + Find Refs/Containers (+ recursive deep descent FindInContainersDeep, 1198) + ScanForValue (lean Candidate V3-A 926; TSet/TMap scan V1a 927) + ScanForValueGroup (multi-value group scan via Orden, 1276) + FindObjectGraphPath (GWorld fwd BFS / Locate in GWorld, 1181; + RecoverViaWorldLevel streaming/WP actor recovery via ULevel::OwningWorld back-ref, status ok_via_level, 1405; + opt-in deep param — follow obj ptrs in one struct-element container level TArray/TSet/TMap<FStruct>, 1780) + GetRelatedObjects (forward owned graph: Self/Class/Outer/Controller↔Pawn/components/ASC/AttributeSet, 1323) + CollectOutgoingObjectPtrs (public outgoing-edge façade for Edel, 1400) + FindInstancesByClass server-side exclude_classes-before-cap + full-pool class_histogram via split loop gated by buildHistogram, 1444) |
|      +-- Serie    (FNamePool)     <- UE5 Pool / UE4 TNameEntry|
|      +-- Ubel     (UStructWalker) <- FField + UProperty + Phases B-K + Optional + ReadFStringAt/ReadFNameAt/ReadFTextStringAt (public, build 757) |
|      +-- Macht    (Memory)        <- AOBScan + SEH read/write |
|      +-- Sein     (Logger)        <- 5-category per-process   |
|      +-- Stark    (GameThreadDispatch) <- MinHook ProcessEvent hook |
|      +-- Radar    (ValueScan)     <- SessionManager + DataType / ScanType / CompareStringPredicate / CompareVectorPredicate (build 738 + Phase 2 757) + GroupSessionManager (multi-value group scan, 1276; `WithSession` = read a session WITHOUT rebuilding/invalidating the cached ordered view, 2715) + the server-side view helpers (`BuildGroupOrderedView` / `GroupTextContainsCI` / `GroupSlotValueString`) + **`PickGroupWitnessAssignment`** — which leaf each slot DISPLAYS (filter-matched > same-struct sibling > any free; no two slots share a leaf). Lives here, NOT in Fern's encoder: `dll_helpers_test` compiles `Radar.cpp` as a source but no test target compiles `Fern.cpp`, and four "the scan missed my field" reports were all this rule drifting from the filter it must agree with (2719) |
|      +-- Orden    (GroupMatch)    <- source-agnostic SDR/assignment matcher for multi-value group scan (header-only, build 1276) |
|      +-- Denken   (NativeDisasm)  <- Zydis x64 decoder: native UFunction -> [this+off] property xref (Path 2, build 862) |
|      +-- Tot      (Cancellation)  <- Tot::Requested() flag; Fern disconnect-monitor + shutdown-abort stop long ops (build 936) |
|      +-- Wirbel   (Teleport)      <- marker save/recall + cursor + camera POV + directional/explicit-coord TP + force mouse cursor (bShowMouseCursor) (build 1144); get_pose surfaces loc/vel FVector owner+offset for per-vector Locate-in-GWorld (1780); CMD_TELEPORT=8 mailbox (ops 0-15) |
|      +-- Solitar  (GodMode)       <- force AActor::bCanBeDamaged (FBoolProperty bit) + re-assert worker; CMD_PROTECT=9 mailbox on/off (build 1251) |
|      +-- Laufen   (MovementTuning)<- force per-pawn CMC float knobs MaxWalkSpeed/GravityScale/JumpZVelocity × multiplier of captured base + re-assert worker (write-on-drift, respawn re-capture); pipe get/set_movement_multiplier/reset_movement + SetKnobPercent (100%=off, jump √height) via UE5_SetMovementPercent export + Mimic CMD_MOVEMENT=10 mailbox (knobId 0-2 float; knobId 3 = GravityDirection FVector UE5.4+, normalized, (0,0,0)=off) for CE Lua/.CT (build 1808) |
|      +-- Hemmung  (TimeDilation)  <- hold reflected time-dilation floats at an ABSOLUTE value (global slow-mo/freeze/speed-up): DIL_GLOBAL AWorldSettings::TimeDilation (GWorld→PersistentLevel→WorldSettings + FindInstancesByClass fallback) + DIL_PAWN pawn AActor::CustomTimeDilation, held by a write-on-drift re-assert worker (absolute-value sibling of Laufen); pipe set/reset_time_dilation + get_time_state + UE5_Set/ResetTimeDilation export + Mimic CMD_TIME=15 mailbox (TimeOp SET/RESET, target 0 global/1 pawn) for CE Lua/.CT (build 2148) |
|      +-- Solide   (ForceField)    <- hold a DISCOVERED reflected field at a value across ALL live instances of a class **AND EVERY SUBCLASS of it** (`Aura::FindInstancesDerivedFrom`, a super-chain DERIVATION test with a per-UClass verdict cache — NOT a name substring, so "Enemy" still excludes "EnemyProjectile"; a Property Search row for an inherited field is keyed to the DECLARING class, so an exact-name pool held nothing, audit #5 A6, 3036) via a write-on-drift re-assert worker (Hemmung sibling; cap 256 — a broad base reaches it, so `truncated` is normal here not exceptional; CDO-skip happens INSIDE the walk, before the cap; re-resolve/tick): bool ON/OFF (reuse Solitar::Set/GetActorBool) / ObjectProperty→null (strong-ptr-only; weak/soft/lazy refused — GObjects[0] trap) / numeric→absolute (LWC-width read/write); best-effort base-restore on RemoveForce. + FindStealthMeter (local pawn+owned components → MatchStealthField keyword-scored numeric field). pipe force_field/reset_field/reset_all_fields/get_forced_fields/find_stealth_meter (pipe-only). Powers Property Search "Force" + Teleport Stealth card — honest subset of "enemies can't detect you" (build 2168) |
|      +-- Edel     (CurrentTarget) <- auto-detect the player's current target: GWorld→PC→Pawn, score outgoing object-ptr fields (structural is-Actor gate + keyword boost); get_current_target pipe-only (build 1400) |
|      +-- Grausam  (ForegroundLock) <- MinHook user32!GetForegroundWindow → always report the game's own window as foreground + subclass game WndProc rewriting WM_ACTIVATEAPP/WM_ACTIVATE/WM_NCACTIVATE→active (two focus channels); defeats UE t.IdleWhenNotForeground idle + focus-loss pause so game-thread invokes/POV keep working when backgrounded; + MinHook ClipCursor/SetCursorPos → release cursor confinement when backgrounded-but-faking-foreground so the mouse isn't trapped in the game window (passthrough when genuinely foreground, 1984); pipe set/get_foreground_lock + Mimic CMD_FOREGROUND=12 mailbox (thread-agnostic, runs on polling thread) for CE Lua/.CT; LIVE-VERIFIED P3R (build ~1950-1956) |
|      +-- Schlacht (SeeThrough)     <- see-through occluders: worker (~10 Hz) traces camera→VIEW-forward (LineTraceSingle, game thread via Stark; works first- AND third-person), hides the nearest **N** non-Pawn/Character hits (configurable pierce depth — iterative single-trace advancing past each impact, no LineTraceMulti/TArray leak) via SetActorHiddenInGame so world geometry stops covering the view; Pawn/Character kept visible + pierced-through via Aura::ClassDerivesFromAny; hit-actor pulled from FHitResult.Actor (UE4 weak-ptr→Aura::GetByIndex) / HitObjectHandle (UE5); restored as the view moves + un-hidden on disable; pipe seethrough_set/get_state (enable+count) + Mimic CMD_SEETHROUGH=14 mailbox (AOBMaker-only CE toggle w/ editable pierceCount); experimental-gated. VERIFIED Tower of Mask + DQ7R; NO-OP on collision/render-split games (FF7R CollisionAssetActor = invisible collision proxy). Build 1991 |
|      +-- Linie    (LivePEProfiler) <- behaviour-based UFunction discovery: opt-in per-UFunction* fire-count table recorded from Stark's ProcessEvent hook during a Start/Stop window (Start → do an in-game action → Stop → see what fired, ranked by count). Hot path free when off (one relaxed atomic gate); dedicated-mutex map, multi-thread-PE safe; UFunction* resolved at query time via Ubel::ResolveFunctionInfo (+ "Function" meta-class stale-ptr guard). pe_profile_start (forces PE hook via UE5_EnsureGameThreadHook)/stop/get; pipe-only, Reset on disconnect (build 2109). + CADENCE (Phase E, build 2156): Welford inter-arrival mean/cv/gap-count per func (fed the timestamp Stark already stamps → zero extra clock reads) → pe_profile_get emits mean_period_ms/cv/gap_samples so the UI flags periodic TIMER callbacks (regular cadence, out of the frame/Tick band) vs input-driven |
+----------------------+-------------------------------+
                       | Named Pipe: \\.\pipe\UE5DumpBfx
                       | JSON over pipe (newline-delimited)
+----------------------v-------------------------------+
|  UE5DumpUI  (Avalonia + ReactiveUI, standalone exe)  |
|      +-- PipeClient                <- async pipe mgmt     |
|      +-- ObjectTreePanel           <- UObject hierarchy    |
|      +-- ClassStructPanel          <- Property Grid        |
|      +-- PointerPanel              <- GObjects/GNames addr |
|      +-- LiveWalkerPanel           <- Instance drill-down (+ browser-style Back/**Forward** history: crumbs popped by Back/breadcrumb-jump go on a forward stack invalidated via `Breadcrumbs.CollectionChanged`; each crumb carries the view state — multi-selection + top-row scroll anchor, reusing the bookmark `CaptureViewAnchor`/`RestoreBookmarkView` pair — restored by a 3-rung ladder (view state → `ScrollHintFieldName` → nothing); `Alt+←/→` + mouse buttons 4/5 + BrowserBack/Forward, gated on foreground + `TabItem.Tag=="LiveWalker"` via pure `Helpers/LiveWalkerNavShortcuts`, 2550) |
|      +-- InstanceFinderPanel       <- Class instance search (+ "Newest first" opt-in: scan GObjects high-index end for just-spawned instances, 1325; + server-side class-noise exclude-before-cap re-run + full-pool class_histogram + configurable Max cap + client keyword filter, 1444) |
|      +-- PropertySearchPanel       <- Search by name (build 610+ dedupe + Freeze button; finder btn + opt-in Deep struct/container descent 1222; + row context "Force" submenu (Solide: Force ON/OFF/→null/value… hold across live instances of the row's class **and its subclasses**, 3036) + "Forced fields (N held)" strip, experimental-gated, build 2168) |
|      +-- InterestingFunctionsPanel <- Scored UFunctions; 📦 batch CT (build 760) |
|      +-- InterestingPropertiesPanel<- Scored properties (B'); 📦 batch CT (build 760) |
|      +-- ConsolePanel              <- UFUNCTION(exec) discovery + UCheatManager hint (build 778) |
|      +-- TeleportPanel             <- marker save/recall + cursor + BugItGo + POV + facing-dir TP + coord TP + force cursor (input-mode, build 1147) + GodMode toggle (Solitar, build 1251) + Move Speed / Gravity / Super Jump cards (Laufen: 10%-1000% log slider × MaxWalkSpeed/GravityScale/JumpZVelocity + Super Jump toggle & global hotkey, build 1792) + DUAL-ROW Time Dilation card (Hemmung: independent World `AWorldSettings.TimeDilation` 0-3× + Player pawn `AActor.CustomTimeDilation` 0-10× levers held SIMULTANEOUSLY — effective pawn rate = world × pawn ⇒ bullet time, "Combined player speed" readout + Player = 1÷World compensation; per-lane slider/presets/Apply/Reset + shared ↻ Refresh both; 2 independently tickable CE records, build 2215) + CE Lua/.CT (17-row) + Global Pointers card: Get GWorld / GameEngine — DLL-invoke via CMD_QUERY_PTR, publish as a registered CE symbol (UE_GWorld = register directly to stable &GWorld slot, auto-follows; UE_GameEngine = &GEngine slot too when its AOB resolved (auto-follows engine recreation), else allocateMemory buffer of a UEngine* snapshot -- decided at ENABLE time, marker symbol tells DISABLE which to free, 2453); AOBMaker push, else paste-able CE-XML clipboard fallback (WrapAaScriptXml); Standalone Trainer card moved to bottom, 1978 + Stealth Meter card (Solide: Detect auto-find → Hold @0 → Reset a stealth/visibility numeric field, experimental-gated, 2168) |
|      +-- ValueSearchPanel          <- CE-style by-value scan (build 738 + Phase 2 757) + Single/Group mode toggle (multi-value group scan, master-detail, build 1276). **A group row shows ONE assignment, not the whole match** — a slot keeps every field that satisfied it (up to `per_slot_cap`, default 256), so the row's `(+35)` annotation (`match_count`, on the wire since 2690, parsed since 2715) and the expanded row's **All fields** button (`query_group_slot_leaves` → every kept leaf BY NAME, each a full slot match so the Live / Addr / Pivot / Locate handoffs apply) are what stop a matched field reading as a missed one, 2715 |
|      +-- RelatedObjectsPanel        <- Related Objects: an actor's class/outer + Controller↔Pawn + owned components/ASC/AttributeSet; 🌍 GWorld / Live Walker / finder handoffs; fed from InstanceFinder/ValueSearch/LiveWalker (build 1323) + 🎯 Detect target auto-detect (Edel, ranked candidate picker, build 1400) |
|      +-- DumpExplorerPanel         <- Offline "Dump All" (.jsonl) browser: ONE keyword search over classes+properties+functions (category filter), split ✅ in current game (class-name-matched via one GObjects pass, restart-safe → 🡒 Jump to Live Walker) vs ⚠ not in game; client-only, no DLL/pipe change (build 2044) |
|      +-- LiveFuncsPanel            <- Live ProcessEvent Call Profiler (Linie): Start/Stop recording → ranked list of UFunctions the game actually dispatched during the window (behaviour-based discovery of OpenShop/Dash etc. that name-scoring can't find); space=AND keyword filter + "Live" (→ Live Walker) / "Name" handoffs; auto-stops recording on tab-leave (build 2109). + Cadence (Phase E, build 2156): "Periodic only" filter + "Timer" Kind badge + Period column flag functions firing at a regular timer cadence (idle-window workflow — the inverse of the action-diff — to find the callback driving a cooldown/respawn/DoT) |
|      +-- ProxyDeployPanel          <- Proxy DLL management + **Leftover proxy cleanup** (find proxies left behind after a game is uninstalled; Report -> .txt dry run + Execute; files to the RECYCLE BIN only, folders pruned leaf->root with NON-recursive Directory.Delete so the kernel enforces "only while empty"; policy is pure in `ProxyOrphanScanner` with zero System.IO, 2525) |
|      +-- CeXmlExport               <- CE XML pointer chains |
|      +-- CsxExport                 <- CE Structure Dissect  |
|      +-- CheatTableBuilder         <- Multi-row .CT batch generator (build 760) |
|      +-- StructReturnDecoder       <- Invoke result property grid (build 775) |
+------------------------------------------------------+
```

-----

## Documentation Index

| Document | Contents |
|----------|----------|
| [docs/roadmap.md](docs/roadmap.md) | **Current state** — capability matrix, per-game configuration, tested games, long-running concerns. **Read first** for "what works now". |
| [docs/tips.md](docs/tips.md) | **User-facing how-to recipes** — goal → which panel/button. First recipe: forcing camera rotation in fixed-view (2.5D/45°) games. |
| [docs/todo.md](docs/todo.md) | **What's next** — prioritized open work with effort/risk tags. Active plan + carryover gaps + speculative ideas. Its `## Pending live-game verification` section is the single owner of "shipped but unproven on a real game" (CI-enforced by `tools/check_live_verification.py`). |
| [docs/pending-verification_zh-TW.md](docs/pending-verification_zh-TW.md) | 🇹🇼 **繁中「怎麼操作」清單 — NOT a translation** (rebuilt 2026-08-17; the translation drifted ~430 builds and was deleted). Every item is a `| # | 做什麼 | 預期 |` table and nothing else: **steps and expected results only**, no background and no root cause — those stay in todo.md, reachable by grepping the finding id. **63 items grouped by what they COST to run**, 第 0 步 (needs nothing at all) through 第 5 步 (no sample exists anywhere — always LOW even when the register says MED, because the absence IS the signal). **todo.md is canonical**; edit there first, then mirror — and when an item closes, DELETE its section here rather than annotating it. |
| [docs/log-verification-checklist.md](docs/log-verification-checklist.md) | **How to sweep a real session's logs** — the procedure half of the register above. Grep by FORMAT STRING, never line number (a 2026-08 survey found every Genau line stale by 12–14 while the strings were exact). Splits **passive** evidence (free from any session) from **deliberate** (what to actually do in-game), plus the traps: markers whose *absence* proves nothing, and the modules that log nothing at all (`Linie`/`Radar`/`Denken`/`Tot`). Two facts that change how you read every log: **there is no log level — nothing is filtered, `[DEBUG]` included**, and **`SEETHRU`/`Grausam`/`SENSE`/`PROXY` fall through `ResolveFile` into `init-0.log`**. §5 is a minimum session that settles the most boxes. |
| [docs/auto-verification-session-plan.md](docs/auto-verification-session-plan.md) | **How to RUN the register unattended** — the operational companion to the two rows above (they own *what* and *why*; this owns *how to drive it with Auto mode + computer-use*). All **64** checklist rows classified by what they actually need: **16 fully headless** (pipe client / log grep / binary probe, no GUI at all), **35 GUI-but-drivable**, **12 needing a bounded human action** (named, with *when*), **1 impossible** (G3 — no forked-engine title installed). Holds the **one up-front `request_access` list** (a mid-run grant request stalls an unattended batch), the batched run order grouped so each environment launches **once**, and §4's **destructive steps that are NOT pre-approved**. ⛔ Two hard rules bound every group: **one game at a time, sequential, never parallel**, and **kill the process when its group ends** — this PC over-loads otherwise. Also records why the Start-menu entries were created *before* the session (computer-use snapshots the installed-app list at startup) and the answer to "must DumperTest be repackaged first?" — **no**, verified by binary probe of both packages. |
| [docs/audit-2026-08-13-early-code-findings.md](docs/audit-2026-08-13-early-code-findings.md) | ⭐ **THE BUG QUEUE — read §3b "▶ THE NEXT FIX SESSION STARTS HERE" if the ask is "carry on fixing"**; when you close one, mark **every individual finding row** ✅ (not just the grouped ① ② ③ row) and run `py tools/check_audit_register.py`, which is CI-gated because this drifted twice — its `--list` prints the open HIGH/MED tier with each row's segment. §3b's fix queue is SPENT (every clump closed, the six-MED dossier archived), so it now routes to todo.md's verification register; open it for a FIX only when the maintainer names a finding. **Bug/leak audit #5 (build 2804) — the EARLY code**, i.e. the one surface audits #3 and #4 structurally could not reach: both were scoped to *"files changed since baseline X"*, so the **48,950 surviving lines authored before 2026-06-01 (37.2% of the tree)** were skipped by each. Scope was **measured, not guessed** — `git blame --line-porcelain` per file, table in §0. Two facts set the priority: `dll/src` holds 45% of it, and the entire C++ suite is **two header-only test files**, so no test target compiles `Ubel.cpp`/`Genau.cpp`/`Macht.cpp`/`Aura.cpp` at all (`Genau`/`Macht` have zero coverage of any kind). Runs as **12 sequential segments** (§1), each = 5 diverse-lens finders → refute-mandated skeptic → second lens on HIGH/MED. **Scanning applies no fixes** — deliberately, per audit #4's own lesson. Working tracker. |
| [docs/audit-2026-08-04-findings.md](docs/audit-2026-08-04-findings.md) | **Bug/leak/refactor audit #4 (build 2554)** — 51 verified items (2 HIGH / 14 MED / 32 LOW / 3 INFO) over everything changed since the audit #3 baseline, in two passes: 4a swept 8 bug + 2 refactor areas, then a completeness critic mapped the holes and 4b closed 6 more. Every item survived a refute-mandated skeptic; HIGH/MED faced a second lens; 7 findings were refuted and are listed as do-not-re-raise. **Two root causes, both worth fixing as patterns**: 4a = *the report and the reality are computed by different code paths*; 4b = *a cheap proxy signal substituted for a predicate a sibling in this repo already computes correctly*. Working tracker. |
| [docs/audit-2026-07-14-findings.md](docs/audit-2026-07-14-findings.md) | **Bug/leak audit #3 (build 2168)** — 32 verified findings (1 HIGH / 10 MED / 20 LOW / 1 INFO) on the post-b1872 Solide/Hemmung/Linie/Schlacht/Grausam + Auto-Snapshot/Dump-Explorer/Live-Funcs code, each with corrected `file:line`, failure scenario, fix shape, effort/risk. Working tracker; the prioritized rollup is in [todo.md](docs/todo.md). |
| [docs/dev-log.md](docs/dev-log.md) | **What shipped** — append-only milestone history per build number. Read when investigating "when/why was X added". |
| [docs/architecture.md](docs/architecture.md) | Directory structure (**31 .cpp + 39 .h** DLL files; **~277** UI .cs/.axaml files; **162** test files; these drift, count them rather than trusting the row), git submodules, build environment. **C++ build is incremental** (Ninja, single build dir, shared `UE5DumperCommon` object lib compiles the proxy-agnostic sources once); `build clean` forces a full rebuild (CI always passes `-Clean`). |
| [docs/dll-spec.md](docs/dll-spec.md) | C++ DLL interface: C ABI exports (**59** — derive it, do not hand-edit; the doc's tables cover the core 30), offset tables, CE Lua bridge (Frieren-themed file/namespace names) |
| [docs/pipe-protocol.md](docs/pipe-protocol.md) | Named Pipe JSON IPC protocol (99 commands incl. Value Search + Group Scan begin/refine/query/end, Request/Response/Event) |
| [docs/multipipe-eval.md](docs/multipipe-eval.md) | **Multi-pipe IPC evaluation** — why NOT to add pipes, and **why the reason changed**. The original root cause (DLL-side serial-dispatch head-of-line blocking) was *reasoned, never measured*; **§10 measured it and refuted it** (build 2324, Elliot + SEED): the dispatcher is idle ~70% of wall-clock and the worst single dispatch out of 24,178 was **14.3 ms**, so there is no head-of-line spike to remove. **Phase 0 and Phase 1 were shipped and then REVERTED** (build 1840, §8) — Phase 0's scan thread-priority drop starved scans ~20×, and it is **not in the tree today** (`grep SetThreadPriority dll/src/` = 0). **Phase 1 = WON'T DO** (§10.1). What did ship is the two-connection **lane split** (PR #396). Read before any pipe/IPC concurrency change. |
| [docs/group-value-scan-spec.md](docs/group-value-scan-spec.md) | **Multiple Values Group Scan** — architecture (`Orden` source-agnostic SDR matcher, block model, deep containers) + the **extension points** for Snapshot/SPC/Pivot reuse + P2/P3/P4 roadmap. Read for "how a future feature plugs into group matching". |
| [docs/snapshot-group-match-spec.md](docs/snapshot-group-match-spec.md) | **Snapshot Group Match (S1–S5 SHIPPED + MERGED main PR #348, in-game VERIFIED)** — N-value group matching over the captured-snapshot corpus via a pure-C# `Orden` port. Mode A single-snapshot absolute + **Mode B cross-snapshot temporal** (per-slot `Changed/Unchanged/Increased/Decreased` — **groups need `Unchanged`**, e.g. Current HP↓ + Max HP unchanged) + **Deep** (nested array/`Tunes[2]` values). Snapshot panel Diff/Group toggle + Compare-with + Deep. Read before touching snapshot multi-value. |
| [docs/native-c-value-scan-spec.md](docs/native-c-value-scan-spec.md) | **Native-C Value Scan (P0–P3 SHIPPED; P3 in-game verify pending)** — opt-in raw/unmanaged (non-`UPROPERTY`) in-object scan for native HP/MP, in Value Search + Group Scan + Snapshot→SPC→Pivot. Reuses "Guess What" (`Ubel::GuessGapTypes`) + the `Orden` seam; holes-only within `PropertiesSize`. Read before implementing native-value scanning. |
| [docs/ui-spec.md](docs/ui-spec.md) | Avalonia UI tech stack (Avalonia 12.1.0 / .NET 10 — version from the .csproj, not this row), AOT compatibility, component specs |
| [docs/export-formats.md](docs/export-formats.md) | CE XML, CSX, SDK Header, USMAP export rules, pointer chain model, type mappings |
| [docs/technical-notes.md](docs/technical-notes.md) | UE version differences, FField vs UProperty, FNamePool, DynOff, Property Type Layouts (Phases B-K), Address Finder layered lookup |
| [docs/lessons-learned.md](docs/lessons-learned.md) | Hard-won lessons from cross-game debugging (20+ games) |
| [docs/working-lessons.md](docs/working-lessons.md) | **How to work on this project** — the sibling of the row above: that one is *what the games do*, this one is *what we keep getting wrong*. Exists because **development runs on two PCs and the assistant's memory files live under `%USERPROFILE%\.claude\`, which git does not carry** — every lesson here was paid for with a debugging session the other machine could not see. §1 verification method (a check must be shown able to FAIL — negative controls, the seam, two detectors, and **a number recorded without its conditions is not a measurement**, including an *absence*, which is how "the corpus is single-copy" got published from the machine that didn't have the backup). §2 **audit finder output is ~52% wrong before refutation** (71/136 over 7 segments; range 33-73%, NOT a constant) and an empty workflow result is what a total wipeout looks like; **§2.1 = what audit #5 measured across all 12 segments**, §2.2 = **what its HIGH tier taught about FIXING** (a finding is evidence a defect exists, not authority on the repair; make a wired-through field `required`; negative-control ONE change at a time; a predicate that guards X belongs WITH X), §2.3 = **what the MED tier taught at VOLUME** (the fix-time sibling grep grew 8 of 12 commits; a finding can be the SMALL half of its own defect; the three C# numeric traps that all lie about whether a `long` survives a `double`; a fixture that silently aliases and reports coverage it does not have), **§2.4 = re-derive the PREMISE, not just the location** (a "vetted" MED can be wrong in the direction that makes a fix DELETE working code — AA4's was), **§2.5 = a rig that RUNS the script beats any number of assertions about its text**, **§2.7 = the offline half of a six-MED session (a per-query CAP makes precision matter only ABOVE it, and that calibration is free over the PE corpus; await what the VM DECIDED to do, not what the test did; a bounded wait, because a hang is not a test result)** (`lua` is installed; `scripts/tests/`). §3 traps in our own stack: we **cannot read our own live log** (`File.ReadLines` vs a writer's handle — silent, and always inside a `catch`), the four Avalonia DataGrid rules, `Microsoft.Data.Sqlite`'s `*Async` that isn't. §4 `FProperty` is **+4 not +8**, the CE Lua quirk set, **§4.3 = `FUObjectItem::SerialNumber` is NOT an identity witness** (two registers in a row prescribed it; UE zeroes it on free and allocates it lazily, so witness the INPUT BYTES of a memoized decode instead — and check whether the memo hands out a VALUE or a REFERENCE before designing any invalidation), and why KismetMathLibrary is not a verification target. §5 the "no scan finds my value" triage (5 reports, 2 causes, neither in the scanner). §6 **settled — do not re-propose**. §7 two-machine operational notes, incl. **§7.1 the memory-routing rule**. **This file is the SOLE copy since 2026-08-15** — it used to be duplicated into the assistant's memory folder on an "edit both" rule, the copies drifted, and that folder does not travel with git anyway. **Write new working-lessons here.** |
| [docs/reference-builds.md](docs/reference-builds.md) | **The engine samples we build ourselves** — 8 UE versions x up to 3 configs, packaged from stock engines as PDB-bearing AOB oracles. Inventory (regenerate with `tools/ghidra/inventory_builds.py`), why each exists, which are deliberately NOT swept and why, and the recipe for making another. Distinct from test-games.md: that asks "what does this GAME do differently", this asks "what does the ENGINE do at version X". |
| [docs/test-games.md](docs/test-games.md) | 30+ test games with UE versions, GWorld status, stride info |
| [docs/naming-convention.md](docs/naming-convention.md) | Frieren-themed C++ file / namespace mapping (Macht/Genau/Aura/Serie/Ubel/Frieren/Fern/...) |
| [docs/aobmaker-integration.md](docs/aobmaker-integration.md) | AOBMaker CE Plugin pipe bridge (HEX / ASM / SYM / CreateAAScript) |
| [docs/mindseye-fork-notes.md](docs/mindseye-fork-notes.md) | **MindsEye licensee-fork notes + RE-DERIVATION PLAYBOOK** — the three things this UE 5.4.4 fork changes (reordered `FChunkedFixedUObjectArray`, 32-byte `FUObjectItem` with `UObject*`@+0x10, XOR-obfuscated `FNameEntry` payloads keyed by a `u16` tag at entry+2), which constants a game patch can move, and step-by-step how to recover each one offline with capstone + `.pdata` (no Ghidra). **Verified on game v7.3.1 only** (PE hash `0863E3B90C993000`). **Read this first if MindsEye breaks after an update.** |
| [docs/reversing-nonstandard-ue-games.md](docs/reversing-nonstandard-ue-games.md) | **Playbook** for games where AOB + heuristics fail (forked/repacked engines): patternsleuth CLI → capstone .data-cluster → Ghidra headless decompile → caller `LEA` → encode the fix. The Avowed (Obsidian, packed 20-byte FUObjectItem) case. **+ Encrypted-games evaluation** — why NOT to vendor Dumper-7/RE-UE4SS for decryption (both are bring-your-own-key hooks; we already have parity via `UE5_SetObjectDecryption`). |
| [docs/corpus-preservation.md](docs/corpus-preservation.md) | **AOB corpus preservation** — the `.rep` is the artifact of record (`sweep.sh` never opens the game binary), so losing a project loses the capability while losing a game only loses re-import. Holds the **PDB checklist** (21 rows / 20 distinct files / 8.11 GB; the 10 Steam-resident ones a patch can destroy), the **re-resolve-by-app-id rule** for titles that reinstall elsewhere, the measured **footprint + ordered drop list** with GB freed and capability lost per step, and the never-drop / unrecoverable set (`ES2-0517.rep` is the last copy of that build). Run `py tools/ghidra/preflight.py` before any sweep. |
| [docs/aob-block-library-eval.md](docs/aob-block-library-eval.md) | **AOB code-block library + n-gram specificity index.** §4 (block library) and §6 (specificity index) are **BUILT and CI-gated** — `.github/workflows/ci.yml` compares `tools/ghidra/blocks/blocks.json` and `tools/pe/aob-specificity-baseline.tsv` byte-wise, so this doc is load-bearing despite the "eval" name. One decision is still open (build-order step 5). |
| [tools/README.md](tools/README.md) | Offline RE helpers — Ghidra scripts (`find_gobjects`/`decompile_functions`/`find_callers` Java + the pyghidra symbol/AOB exporters) and a capstone PE disassembler (`pe/disasm_function.py`). |
| [scripts/analysis/README.md](scripts/analysis/README.md) | Offline analysis tooling — `analyze_dumps.py` (cross-game keyword calibration) + `diff_dumps.py` (same-game patch diff, build 780). |
| [docs/simd-scanning-notes.md](docs/simd-scanning-notes.md) | AOBMaker SIMD scanning research: AVX2 anchor technique (reference) |
| [docs/CE-Bugs-Minesweeper.md](docs/CE-Bugs-Minesweeper.md) | **CE bugs and undocumented behaviours we actually hit** — six items, re-verified 2026-08-07 against source `7.5-195` / binary `7.7.0.10568` (both coordinates now in the header; each item states which one it was checked against, because **the public source lags the release** and "absent from the source" does not prove "fixed in 7.7"). Type 7's `specialstringpointer` is dead because CE copies to a `TStringList` at `disassemblerviewlinesunit.pas:496` *before* dispatching at `:952` — all five cited lines re-confirmed exact. §4's r/m16 `imm8` sign-extension bug is the worked example of why: it is **still present in the last public source** but **measured FIXED in the 7.7 binary** (`cmp bx, AA` → `66 81 FB AA00`), so the source alone would have had the doc assert a defect the shipping CE does not have. §3 was **reframed**: there is no `enum MemRecValueType` in the SDK header to be wrong — it was a stale `//` comment, and 7.7 corrected it. §5's title says "cached" but nothing caches the file: CE has one Lua state it never rebuilds, and `resetLuaState()` clears it without a restart. **§6 (added 2026-08-17) is the one that has already cost us**: `celua.txt` says `errorOnLookupFailure` defaults to TRUE, `TSymhandler.create` sets it FALSE, so `getAddress` RETURNS 0 rather than raising — an audit finding built on the doc would have deleted a live guard. |
| [docs/ce-plugin-api-reference.md](docs/ce-plugin-api-reference.md) | **CE Plugin SDK C ABI reference.** All 159 `ExportedFunctions` members in order, the nine plugin types, every enum, the `pluginsync` threading model. Head of the five-doc **CE reference set** below — all five are **MIRRORS** of `D:\Github\CE-Handwire-Private\docs\`; **edit the master first, then mirror**. Re-verified claim-by-claim against CE source tag **`7.5-195`** (2026-08-07) and cross-checked against the installed **7.7.0.10568** binaries. The load-bearing compatibility result: **the plugin C ABI is identical in 7.5 and 7.7** — `cepluginsdk.pas` is byte-identical, `cepluginsdk.h` differs on exactly one comment line — so one plugin binary serves both. ⚠ **Every count and `file:line` in all five comes from an EXTERNAL tree, so `tools/check_derived_counts.py` structurally cannot guard them** (it only derives from this repo). Re-grep the identifier; never trust the line number. |
| [docs/ce-plugin-sdk-notes.md](docs/ce-plugin-sdk-notes.md) | **The pitfalls companion** — what CE's Pascal does that its C header does not admit. Governs what this repo emits: CE's real `TVariableType` ordering (the SDK header declares **no** value-type enum at all — the 7.5-era `//` comment was simply wrong and 7.7 fixed it), `previousOpcode`/`nextOpcode` returning `ptrUint` where the header says `DWORD`, and the Lua-state threading rules. **§13 owns `executeCodeEx`**, which every `callDLL` we emit runs through: its wait is a `WaitForSingleObject` **on the calling thread with no message pump**, so the timeout is a ceiling on GUI-freeze time and a Lua-side `processMessagesPaintOnly` structurally cannot reach it (unlike the `sleep()` mailbox loop, which pumps itself); a `nil` timeout means **INFINITE**, not "default"; and CE returns `nil, reason` with six distinct reasons — every call site captures the reason as of build 2792, and re-introducing a guessed message is the regression to watch. |
| [docs/ce-ccode-eval.md](docs/ce-ccode-eval.md) | **`{$CCODE}` adoption — EVALUATED 2026-08-07, DO NOT ADOPT.** Not "small win" — **structural**: the repo emits **zero injection hook sites** (all 31 `{$asm}` are bare terminators of a `{$lua}` block; zero `aobscanmodule`/`newmem`/`jmp`), and CCODE without a `call` into its stub is dead text. The trap the doc exists to disarm: **`{$lua}` ≠ `{$LUACODE}`** — ours runs once in **CE's** process at assemble time; CCODE competes with `{$LUACODE}`, which runs per-hit in the **game**. Cost ladder: hand-written asm < CCODE < LUACODE, the gap being the SafeCall stub (`fxsave`/`fxrstor` + 15 GPRs, every hit) — and **our injected DLL pays no stub at all, so it is faster than CCODE, not slower**. Holds the pros & cons, the refuted best case for adoption (standalone teleport snap-back is missing `ComponentToWorld` propagation, not a race — and `Wirbel::DeepForceWorldPos` already solves it hook-free), the two falsifiable conditions that would reopen it, and §9's incidental finds. Corrects a natural guess: CCODE and LUACODE **shipped in the same commit** (`8074febf`, 2021-01-21) sharing one `AddSafeCallStub` — CCODE was not built to fix a slow LUACODE. |
| [docs/ce-ccode-reference.md](docs/ce-ccode-reference.md) | **`{$CCODE}` / `{$C}` manual — the native-code alternative to the `{$lua}` blocks this repo generates** (`Services/CeLuaHygiene.cs` + the `*ScriptGenerator.cs` family). Same AA hook slot, compiled by CE's bundled TCC, no interpreter round trip per hit. §4's parameter-pointer layout is shared with `{$LUACODE}`, so it already describes the register marshalling behind what we emit; §12 is the direct comparison. Also documents four **CE-side defects** that bite anyone using it literally (comma-separated params silently dropped; a mistyped register silently binds to RAX; the `RBPF`/`RSPF` float aliases land on the wrong slots; `PREFIX=` on a `{$CCODE}` line injects a phantom variable). |
| [docs/ce-memory-scanning-internals.md](docs/ce-memory-scanning-internals.md) | **How CE's scanner actually goes fast** — the reference implementation our own scanners (Radar value scan, `Aura::ScanForValue`, `Macht` AOBScan) are measured against. §14 is the point: CE's speed is **alignment stride + thread fan-out, not a clever inner loop** (`DWordExact` is one load and a `CMP`; there is no SIMD anywhere). Also the buffer/overlap model, the nibble-wildcard encoding, and §15 on AOBMaker's `Vector256` anchor scan. **§16 of the master is deliberately not mirrored** — it documents another project. Sibling of [simd-scanning-notes.md](docs/simd-scanning-notes.md). |
| [docs/ce-disassembler-navigation.md](docs/ce-disassembler-navigation.md) | Driving CE's Memory Viewer from outside — the verified `getMemoryViewForm().DisassemblerView.SelectedAddress` route (backed by the real published Pascal property at `disassemblerviewunit.pas:209`, not just by `celua.txt`), reusable from the `{$lua}` blocks this repo emits. Includes why the Type 6 pointer-write trick does **not** survive into a deferred context, and why writing an address back is not the no-op it looks like. |
| [docs/teleport-spec.md](docs/teleport-spec.md) | **Teleport / Wirbel design contract** — markers, POV, coord TP, cursor forcing, the `CMD_TELEPORT` op table. The most-referenced spec in `docs/` (9 inbound links). |
| [docs/teleport-coord-library-spec.md](docs/teleport-coord-library-spec.md) | **Teleport Coordinate Library — P1–P5 SHIPPED (builds 2257-2267); the DLL-flavour picker is in-game verified, the standalone (no-DLL) flavour is NOT.** An unlimited labelled+grouped coordinate list with pick→confirm→teleport, CE-Lua export (needs-DLL / no-DLL) and copy-paste re-import. Core needs **zero DLL/pipe change**. Holds the locked decisions (module-name file key, Map as a first-class filter, embedded-in-script dataset vs the verified 10 MiB cap), the **verified CE Lua control set** for the picker form, and the **character-blocking rule** (`NUL` / CR/LF / `]==]`). Sibling of [teleport-spec.md](docs/teleport-spec.md). |
| [docs/godmode-spec.md](docs/godmode-spec.md) | **GodMode / Solitar design contract** — the invincibility-bool scan + re-assert model, and the **locked Non-Goal** (no universal detection bool; surface per-game via Property Search) that also governs `Solide`. |
| [docs/log-compression-eval.md](docs/log-compression-eval.md) | **Log compression — SHIPPED build 2730 (`compact /c /exe:LZX`, in place).** Measured on the real 111.9 MB log folder: LZX saves **94.1 MB of 102.05 MB in 2.8 s** (12.8:1), and **`LastWriteTime` is preserved on all 289 files** so the 21-day purge needs zero change. gz/zip compresses 1.6% better and breaks the purge globs, grep, and "Open Log Folder" — rejected. Two triggers: System-tab **button** (idle ≥1 h) and an opt-in **startup sweep, default OFF** (≥7 days). **In-app verified**: 180 files, 85.1 → 6.5 MB. §5a: **`-0.log` is a slot name, not a liveness fact** — only the UI's OWN folder is excluded by identity; a game's newest log is eligible after 7 idle days (the sibling purge already DELETES those on age alone, and rename preserves LZX). Holds the six traps (quote paths or files are SILENTLY skipped; `FileAttributes.Compressed` is not set by LZX; appending decompresses) and the reason not to add I/O throttling. |
| [docs/text-translation-eval.md](docs/text-translation-eval.md) | **In-game S2T conversion + local-LLM translation — EVALUATED (2026-07-24), in-memory rewrite REJECTED.** The three UE-source-level walls that kill live text-rewrite (ProcessEvent can't see `SetText`; same-length overwrite doesn't repaint; `FString::Data` can't be repointed) + the first-order font-glyph risk; why the **offline `.locres` route wins** and LLM belongs in an offline pre-pass; the recommended `Lektüre` reader module; and a one-page offline how-to. Phase 3 FText UTF-8-read fix shipped in build 2368. |
| [docs/experimental-snapshot-spc-pivot.md](docs/experimental-snapshot-spc-pivot.md) | Snapshot / SPC / Class Pivot — the experimental-tab family: capture model, intersection queries, pivot aggregation. |
| [docs/ce-export-drilldown-spec.md](docs/ce-export-drilldown-spec.md) | CE-export pointer drill-down spec (depth model, cascade resolution) — the companion to [export-formats.md](docs/export-formats.md). |
| [docs/avowed-gobjects-fix.md](docs/avowed-gobjects-fix.md) | The Avowed case study — static `FUObjectArray` + 20-byte packed `FUObjectItem` + the GWorld decoy. Read alongside [mindseye-fork-notes.md](docs/mindseye-fork-notes.md) for non-standard-layout work. |
| [docs/archive/](docs/archive/) | Superseded docs + the older `dev-log` halves, preserved for history. See its [README](docs/archive/README.md) for why each was archived. **`dev-log.md` holds builds 2200+; 1800–2168 are in `archive/dev-log-2026-07-pre-build-2200.md`, 1178–1799 in `archive/dev-log-2026-06-pre-build-1800.md`.** Two closed `todo.md` sections (audit #3 rollup, winmm proxy) are in `archive/todo-closed-2026-08-build-2715.md`. The **spent six-MED re-derivation dossier** (PX1/A3/AF3/U3/V3/V4 — all fixed, builds 3166–3171) is `archive/audit-2026-08-16-med-rederivation.md`. |
