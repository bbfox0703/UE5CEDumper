# Recommended Test Games

> **Real shipped titles only.** The stock engine samples we package ourselves (8 UE versions x up to 3 configs, used as PDB-bearing AOB oracles) live in
> [reference-builds.md](reference-builds.md) — different subject, different questions.

> Moved from CLAUDE.md. A curated list of games used to validate UE5CEDumper across UE versions.

-----

| Game | UE Version | Notes |
|------|-----------|-------|
| EverSpace 2 | ⚠ **PATCHED 2026-09-01: the installed exe is now UE5.6.1 (PE: 506)** — measured 2026-09-05 from `dwProductVersionMS`, the field `DetectVersionFromPEResource` reads. Everything right of here was measured on the **5.5.4** build, which is archived under `Game archive\ES2\`. Re-run before citing any of it. ~~UE5.5 (PE: 505)~~ | GNames via pointer-scan fallback. Stride 24, 1.16M objects. GWorld ✅ (build 1.0.0.27) |
| Titan Quest II | UE5.7 (PE: 507) | ⚠ **CPN claim CONTRADICTED 2026-08-14** — a live inject logged `DetectCasePreservingName: votes standard=20, CPN=0`, `CasePreservingName: no`, with UE 507 and `FUObjectItem size=24 (UE5.7+ reordered)` all resolving cleanly. Either the game was patched or this row was always wrong; see todo.md. ~~CasePreservingName + DynOff. Stride 16.~~ 486,782 objects. GWorld ✅ via fallback ([GWorld]=0, UWorld found in GObjects) |
| OctoPath Traveler | **UE4.18** (was recorded as "4.22 (inferred)" — corrected 2026-07-27, confirmed three ways: the `++UE4+Release-4.18` build tag in `.rdata`, a pattern fingerprint identical to DQ XI S (a known 4.18: GNames 7/28 hit, sparse 0/10), and the live DLL reporting `UE418`) | 406060 objects. GNames via GNAM_CT3 ✅. GObjects via GOBJ_RE2 ✅ (Flat FFixedUObjectArray, validated by "Flat" preset). GWorld via GWLD_TQ_1 ✅. Codename "Kingship". Ghidra: GObjects RVA `0x29E5C20`, GNames RVA `0x29DCF08` (TNameEntryArray stride 0x4000). GOBJ_OT_1/OT_2 also added but untested (lower priority than RE2). **PROXY: use `winmm.dll` — LIVE-VERIFIED 2026-08-18** (180/180 exports forwarded, pipe up, full scan: GObjects/GNames/GWorld/GEngine all `aob`, **273,956 objects**, 0 errors in all 5 logs; `[OCTOPATH-G2T3-2026-08-18]`). ⚠ **The conclusion is right but this entry's former REASONING was wrong in both halves, and the reasoning is what gets reused to predict other titles.** Corrected by parsing the import table (PE32+ data directory 1): the exe has **42 static imports**, and it **DOES import `VERSION.dll`** — three functions, `VerQueryValueW` / `GetFileVersionInfoW` / `GetFileVersionInfoSizeW`. `WINMM.dll`'s imports are `waveOutGetNumDevs` + `timeBeginPeriod`, **not** the `GetVersionExW`/`VerifyVersionInfo*` set this entry used to attribute to it (those are kernel32 APIs). So "it does not import version.dll" was false, and "winmm works because its imports are all `GetVersion*`" was false. ⇒ **Why the app-directory `version.dll` proxy loses is NOT established** — the loaded `VERSION.dll` is System32's and our copy logs nothing (the silent-total-failure signature), but a KnownDLLs explanation was tested and **refuted** (`version.dll` is not in the KnownDLLs list and no KnownDLL imports it). Do not publish a cause for it; rely on the measured outcome. `dxgi.dll` is imported (`CreateDXGIFactory1`/`CreateDXGIFactory`) but CRASHES here — it loads under the early loader lock, see the dxgi thin-shim item in todo.md. |
| Final Fantasy VII Rebirth (FF7Re) | UE4.26 fork | Hash-prefixed FNameEntry (hdrOff=4, stride=4) + stride 24 — fully working. GWorld ✅ |
| Final Fantasy VII Remake Intergrade (FF7R) | UE4.18 fork | Flat FFixedUObjectArray ✅. UProperty fallback ✅. 315304 objects. GWorld ✅. Version: flat+UProperty → 418. Base GameInstance (9 fields, no BP subclass) |
> ⚠⚠ **VERSION ROWS: RECONCILE, DO NOT TRUST — `py tools/verify/test_games_reconcile.py --quiet`.**
> Added 2026-09-06, after `DQ XI S` was found stating **UE4.22** for six weeks while its own
> `.rdata` carried `++UE4+Release-4.18` — and while **line 14 of this very file** already called it
> "a known 4.18". Two rows of one table disagreeing, with the right answer present the whole time.
>
> ⛔ **There is no CI gate for this, and the reason is measured rather than assumed.** No version
> signal exists on every machine, because the games are not in the repo:
> * the authoritative `++UE4/5+Release-X.Y` build tag is present in only **2 of 19** exes here;
> * the game exe's PE ProductVersion is frequently the **game** version, not the engine —
>   OCTOPATH 1.0, DQ7R 1.1, Elliot 1.2, TQ2 63339.64744;
> * `CrashReportClient.exe`'s ProductVersion is **engine-shipped** and the best broad signal at
>   **7 of 15** titles, 6/7 exact.
>
> So the answer is a rerunnable rig, not a gate. Run it after a game patch or a detection change.
>
> ⚠ **Never infer a version from a fork's LAYOUT.** DQ XI S's `+0x10` shifted UObject layout and
> `UField::Next=+0x38` are a **licensee fork**, not a version signal; reading "newer than stock
> 4.18" out of them is exactly what produced the wrong number.
>
> ### ⭐ The authority order, strongest first
>
> | # | signal | where | why it ranks here |
> |---|---|---|---|
> | 1 | `++UE4+Release-X.Y` / `++UE5+Release-X.Y` | the exe's `.rdata` | the branch the binary was **built from**. Absent in ~90% of shipping titles, and that is its only weakness |
> | 2 | `UE4PrereqSetup_x64.exe` vs `UEPrereqSetup_x64.exe` | the game folder | **UE4 vs UE5 only**, but on that question it is decisive — UE5 dropped the `4`. Present in ~7 of 16 titles here, 6 of 6 comparable ones correct |
> | 3 | `CrashReportClient.exe` ProductVersion | `Engine\Binaries\Win64\` | shipped **by the engine**, not by the game, so unlike the game exe it is not the *game's* version. ~7 of 15 titles, 6/7 exact |
> | 4 | our own `Genau::DetectVersion` | `scan-0.log`, and the per-PE-hash cache | not independent — it is the thing under test — but it is what the tool will actually DO on this binary |
> | ✗ | the **game exe's** PE ProductVersion | — | frequently the GAME version: OCTOPATH 1.0, DQ7R 1.1, Elliot 1.2, TQ2 63339.64744. Never rank it above the four above |
> | ✗ | the **layout** (stride, `FFieldVariant` width, `UField::Next`) | — | ⛔ **not a version signal at all.** A licensee fork changes the layout without changing the engine, and reading "this looks newer than stock" out of it is exactly what put UE4.22 on the DQ XI S row for six weeks |
>
> ⚠ **Two signals agreeing beats one signal plus an inference**, and an inference from the layout is
> not a signal. Where the top four disagree, the higher number wins and the row should say so.
>
> **Still open — needs a boot, do not "fix" it from the table alone:**
> * **Avowed** — this file says UE5.3 (PE: 503); the DLL's own detection cache holds **504**.
>
> ℹ️ A disagreement is a **question**, not a verdict. DragonSword Awakening reports CRC 5.3 against a
> DLL 504, and that is correct and already documented — the 504 is our own runtime raise.

| DQ I&II HD-2D Remake | **UE4.27** — corrected 2026-09-06 (was "UE5.05 (detected)") | ⛔ **Three independent file signals say UE4, and the old value crossed the UE4/UE5 boundary in the wrong direction**: the folder ships `UE4PrereqSetup_x64.exe` (UE5 ships `UEPrereqSetup_x64.exe`), `Engine\Binaries\Win64\CrashReportClient.exe` has ProductVersion **4.27**, and `Genau::DetectVersion` currently reports **427**. An engine does not go backwards, so the historical `UE505` was a **misdetection recorded as fact** — the same shape as DQ XI S. ⚠ **The layout notes below were written under that wrong version and are NOT re-derived**: Stride 24, 128678 objects, GWorld ✅, FFieldVariant=0x10 with the Step 6.5 inference (Name=0x28 from Next=0x20), BP_SantiagoGameInstance_C (64 fields). Re-check them on the next boot. |
| DQ III HD-2D Remake | **UE4.27 (INFERRED, not measured)** | ⚠ **Not installed on either machine, so nothing here was measured.** The version follows DQ I&II purely because this row already recorded "same SE HD-2D fork layout" — which is an inference from LAYOUT, the one thing the authority order above says is not a version signal. Treat as unknown until the folder can be checked for `UE4PrereqSetup_x64.exe` / `CrashReportClient.exe`, which needs no launch. Historical notes, written under the old UE505 reading: 126022 objects, GWorld ✅, FFieldVariant=0x10 inference fix applied. |
| DQ XI S: Echoes of an Elusive Age | **UE4.18** (was recorded as UE4.22 — **corrected 2026-09-06, measured**: the binary carries `++UE4+Release-4.18` in `.rdata`, byte-identical to OCTOPATH's tag, and the live DLL reports `UE418`. This file already said so at `:14`, which calls DQ XI S "a known 4.18" and fingerprints OCTOPATH against it — the two rows contradicted each other. ⚠ The `+0x10` shifted layout and `UField::Next=+0x38` are a LICENSEE FORK, not a version signal; reading them as "must be newer than 4.18" is what produced the wrong number) | 350251 objects. GWorld ✅. UField::Next=+0x38 (non-standard, probed). UProperty mode. Expanded UObject layout (+0x10 shift). BP_GameInstance_C (99 fields), BP_GameFlag_C (10 fields) |
| Tower of Mask | UE4.27 | Standard UE4 indie game — full pipeline confirmed working. Stride 24. GWorld ✅ (build 1.0.0.27) |
| Hogwarts Legacy | UE4.27 (PE: 427) | GNames via pointer-scan fallback. Stride 24, 379K objects. GWorld ✅ (build 1.0.0.27) |
| IDOLM@STER STARLIT SEASON | UE4.24 | Working. GWorld ✅ (build 1.0.0.27). CDO skip fix effective |
| Romancing SaGa 2 | UE4.27 | Working (build 1.0.0.27). GWorld ✅ |
| Star Wars Jedi: Fallen Order | UE4.21 | Working — 313 887 objects, **GWorld ✅** (`0x7FF7317EBAB8`) re-verified build 704 (2026-05-12) via CE injection. EA-launcher title: `version.dll` / `dinput8.dll` proxies do NOT load (EA app restricts the DLL search path); must inject via CE after the game is running. Install at `SwGame\Binaries\Win64\` holds TWO 58.4 MB exes side-by-side (`SwGame-Win64-Shipping.exe` + `starwarsjedifallenorder.exe`); CE sees the launcher exe as the running process. |
| Ghostwire: Tokyo | UE505 detected (possibly UE4) | Working (build 1.0.0.27). 254493 objects. GWorld ✅. UE version likely incorrect. RE-UE4SS has only AOB signatures, no version override |
| Lushfoil Photography Sim | UE5.6 (PE: 506) | NEW (build 1.0.0.40). All working. 58630 objects |
| Manor Lords | UE5.5 | NEW (build 1.0.0.40). All working |
| Satisfactory (v1.1.3.1) | UE5.3 (PE: 503) | Working — modular UE build with separate `FactoryGameSteam-CoreUObject-Win64-Shipping.dll` under `Engine\Binaries\Win64\`. `Macht::AOBScanAllModules` falls through to the CoreUObject DLL; the 15-game dump corpus (build 678 + 687) contains its 4 868 BPGCs cleanly. **GWorld ✅** re-verified build 704 (2026-05-12); the original "GWorld fails" note was stale (pre-`AOBScanAllModules`). Proxy deploy fixed build 691 — UI now finds the launcher at `Engine\Binaries\Win64\FactoryGameSteam-Win64-Shipping.exe` (real .exe is in `Engine\`, not `FactoryGame\`). |
| Satisfactory (v1.2.3.1) | UE5.6 (PE: 506) | NEW — re-verified after the **Satisfactory 1.2.3.1 update bumped the engine UE5.3 → 5.6** (build 1.0.0.1718, 2026-06-25, log dir `FactoryGameSteam-Win64-Shipping`). **Zero code changes vs the v1.1.3.1 / UE5.3 row above** — dynamic version detection + symbol-export globals + DynOff handled the bump transparently. Loaded via **`version.dll` proxy** in `Engine\Binaries\Win64\` (`FactoryGameSteam-Win64-Shipping.exe`, Steam, PID 4048). **UE version auto-detected as UE506** (memory scan, not hardcoded; CPN votes standard=20 / CPN=0). **All three globals resolved via symbol export — no AOB needed**: GObjects `?GUObjectArray@@3VFUObjectArray@@A` from `FactoryGameSteam-CoreUObject-Win64-Shipping.dll` (`0x7FFB9BC13620`), GNames via `?ToString@FName@@QEBAXAEAVFString@@@Z` from `…-Core-…dll` + SymbolCallFollow LEA (`0x7FFB9C49E8C0`), GWorld `?GWorld@@3VUWorldProxy@@A` from `…-Engine-…dll` (`0x7FFB905BDB68`). **217,602 objects** (Max 2.16M / 4 chunks), **FUObjectItem 24 B** (UE5-Extended, strict; stride-24 wins 200/200). DynOff: **CasePreservingName=no**, FProperty + **TaggedFFieldVariant (UE5.3+)**, all offsets standard UE5 — Outer +0x20; UStruct Super/Children/ChildProps/PropsSize/Script = 0x40/0x48/0x50/0x58/0x60; FField Class/Next/Name = 0x08/0x18/0x20; FProperty ElemSize/Flags/Offset = 0x34/0x38/0x44; FStructProp::Struct +0x70. FNamePool hdrOff=0 stride 2, BlockOffsetBits 16, Format A (shift 6). SparseDelegates via `SPARSE_ES2_1` AOB (multi-module). **GWorld walk ✅** — UWorld `World` 39 fields (propsSize 2736), PersistentLevel @+48, `FGGameInstance` 21 fields, WalkFunctions OK. Confirms the RE-UE4SS UE 5.6 deltas (GMalloc → `UE::Private` namespace, StaticConstructObject_Internal new signature) don't touch us — a read-only dumper uses neither. |
| DragonSword Awakening | UE5.4 (PE: 503 → runtime-raised to **504** by the `CMC::GravityDirection` property marker) | NEW (build 2779, 2026-08-10, log dir `DSClient-Win64-Shipping`). Everything resolved on a stock layout — **GObjects ✅** — see the ⚠ below: the address always resolved, but until build 2782 the *layout* behind it was misdetected. **The winning pattern and ANCHOR are not stable across runs on this title** (same PE hash, same module base `0x7FF61C010000`): build 2780 got `GOBJ_ES53_1` → `0x7FF62529F8B0`, the `FUObjectArray` **base**; build 2786 got `GOBJ_V13` (193 matches, hint-cache MISS on `GOBJ_ES53_1` first) → `0x7FF62529F8C0`, i.e. **`ObjObjects`, +0x10 further in**. Same array either way — it just decides which preset row has to match, which is exactly how the base-anchor run fell into the *relaxed B* trap, **GNames ✅** `GNAM_V8` (chunks@+0x10, hdrOff=0, FmtA), **GWorld ✅** `GWLD_TQ_1`, SparseDelegates ✅, and the **&GEngine slot ✅** `GENG_X1` (`0x7FF62540A710` → engine, gvOff 0xA58, viewport validated). ProcessEvent at vtable+0x268 (offset 616), hook validated at 9 540 fires/1500 ms. DynOff standard UE5: CPN=no, FProperty + TaggedFFieldVariant, Outer +0x20, UStruct 0x40/0x48/0x50/0x58/0x60, FField 0x08/0x18/0x20, FProperty 0x34/0x38/0x44, FStructProp +0x70; `UEnum::Names` at UEnum+0x40 (legacy TArray). Zero errors in the whole session. **PROXY: use `dxgi.dll`. `version.dll` does NOT load — verified, and it is the honest counterexample to the "version works anyway" rule.** The exe imports `dxgi.dll` + `WINMM.dll` (and `XINPUT1_3`/`DSOUND`/`OPENGL32`) but **not** `version.dll`; unlike the 11 games where a version proxy still loads via a run-time `LoadLibrary`, nothing in this process ever asks for it by bare name, so the version proxy sits there producing **ZERO log** — the silent-total-failure signature. `dxgi` forwarded 20/20 exports lazily and is stable here (contrast Octopath, where dxgi is imported early enough to instant-exit). Note the game folder name contains **TWO spaces**: `DragonSword  Awakening`. **⚠ This is the title that exposed the GObjects layout misdetection fixed in build 2782** (see [dev-log.md](dev-log.md)): it sets a generous `gc.MaxObjectsInGame` (MaxChunks **161** → MaxElements **10 551 296**), which blew past the old `0x800000` MaxElements cap, so the correct preset was rejected in the strict tier and both validators fell back to the relaxed one. On the base anchor that meant *relaxed B*, reading `NumElements` out of `ObjLastNonGCIndex`. The result looked healthy and was logged as a success: a frozen **37 099** objects (startup disregard-for-GC CDOs only) instead of the live count, so no runtime object was visible to any scan — Value Search, Instances, Property Search and snapshots all saw CDOs only.<br>**VERIFIED FIXED in-game, build 2786 (2026-08-10).** `ValidateGObjects: Valid at 0x7FF62529F8C0 (preset Default, Num=266614, Max=10551296, Objects=0x24C0D944BD0)` + `ObjectArray: Layout 'Default' detected (strict, preset 1/5)` — **accepted by the STRICT tier, which the old 8.4 M cap made impossible**, so this line alone proves the ceiling fix took. Live `NumElements` **266 614 at init → 274 900** later in the same session (it moves; that is the whole tell). The original repro now returns the row: `DsClientLocalPlayer.<raw@0xAC0>` = 9338, Native-C, on the live `DsClientLocalPlayer_2147482392`. Caveat on what this run does **not** prove: it resolved the `ObjObjects` anchor, where even the old relaxed `A/C` row would have read the right `NumElements` — so the *relaxed-B-steals-row-E* half of the fix is verified only by construction, not by this session. Since the anchor varies per run (above), a future run on this title can still land on the base and exercise it.<br>Array layout, `FUObjectArray` base-relative: Objects@+0x10, MaxElements@+0x20, NumElements@+0x24, MaxChunks@+0x28, NumChunks@+0x2C (MaxChunks 161, NumChunks 5, `FUObjectItem` stride **24**, classic `UObject*`@+0x00).<br>**Run 3 (build 3122, 2026-08-16) — a THIRD distinct anchor+preset, which strengthens the "not stable across runs" note above rather than contradicting it.** `GOBJ_ES53_1` again, but **40 matches** this time (vs the earlier runs' counts) → `0x7FF7C79E1870`, accepted in the **STRICT** tier as preset **`UE5-Extended`** (`Num=278252, Max=10551296, Objects=0x1B5BFC1A8D0`), where build 2786 was accepted as preset `Default`. So all three runs agree on the *array* and disagree on *which preset row matches*, which is precisely the failure surface the 2782 ceiling fix was about — and the strict tier has now accepted this title under two different presets, neither of them relaxed. Module base `0x7FF7BF…` (ASLR; the earlier note's `0x7FF61C010000` is not fixed either). Everything else reproduced exactly as recorded: GNames `GNAM_V8` (1 hit, `chunks@+0x10, hdrOff=0, FmtA, 'None'`), GWorld `GWLD_TQ_1`, Sparse `SPARSE_ES2_1` (num=1695), GEngine slot `GENG_X1` → `0x7FF7C7B4C6D0` (engine `0x1B84B9CEEF0`, gvOff `0xA58`, viewport validated), version `PE VERSIONINFO -> 503` then raised to **504** by the `CMC::GravityDirection` marker. **Zero errors and zero `[WARN]` in a 2.4 MB `walk-0.log`** covering a 35,891-object / 2,917,264-field snapshot capture. |
| Cat Island Petrichor Demo | UE5.6 | Full working. GWorld ✅ |
| Way of the Hunter 2 Demo | UE5.7 | Full working. GWorld ✅ |
| COMBAT PILOT: CARRIER QUALIFICATION Demo | UE5.5 | Full working. GWorld ✅ |
| Everspace 2 (ES2-0517 snapshot) | **UE 5.5** (exe `ES2-Win64-Shipping.exe`; Ghidra project `ES2-0517.rep` — **`0517` is a DATE, not a version**) | **OFFLINE ONLY** (no game install / no EXE on disk any more). **Ships a full PDB — the project's SECOND symbolised oracle** (507K functions / 5.84M symbols). Version pinned structurally, not by string (no `++UE5+Release-` in the image): `FFieldVariant`=0x08 (≥5.1.1), `UEnum::Names` still `TArray<TTuple<FName,int64>>` (<5.6), `FUObjectItem` 24B **with `RefCount`@+0x14**, classic `FChunkedFixedUObjectArray` order (<5.8), and the PDB's `EUnrealEngineObjectUE5Version` enum ends at `ASSETREGISTRY_PACKAGEBUILDDEPENDENCIES` (UE 5.8 adds `METADATA_SERIALIZATION_OFFSET`/`VERSE_CELLS` after it). Ground truth: `GUObjectArray` `0x149AA7EE0`, `NamePoolData` `0x149C009C0`, `GWorld` `0x149B37D18`, `SparseDelegates` `0x149AA7E90`, `GEngine` `0x149DA5810`. Audit: GObjects/GNames/GWorld/Sparse all covered, but **GEngine had only 1 of 4 patterns hitting** → source of `GENG_ES55_1` (build 2401), which also covers 5.7. |
| Everspace | **UE 4.20** (exe `RSG-Win64-Shipping.exe`, md5 `d6e8511c…`; Ghidra project **`ES1-420.rep`** — re-analysed WITH the PDB; the older symbol-less `ES1.rep` is superseded) | **OFFLINE ONLY** but fully symbolised (128,657 functions / 1,411,120 symbols) — the project's oldest engine sample. Ground truth: `GUObjectArray` `0x142E797F0`, `GWorld` `0x1432E1AC0`, `GEngine` `0x1432DF470`, and the UE4 name array `Names` `0x1431DEAD8` (recovered from `FName::GetNames` @ `0x1406B19D0`, which lazily `new`s a 0x408-byte `TNameEntryArray`). No `NamePoolData`/`SparseDelegates` — 4.20 predates both FNamePool and sparse delegates (4.23+). **Validated the pattern-consensus technique**: before the PDB existed, running the whole signature database and taking addresses that ≥3 independent patterns agreed on gave GWorld/GObjects/GNames — and all three matched the symbols exactly. Coverage: GWorld ✅ (`GWLD_TQ_1`, correct-first), GEngine ✅ (`GENG_X1`, correct-first), GNames ✅ but only via the last-resort band (`GNAM_CT3` pri 800 / `GNAM_G42_1` pri 840 are UNIQUE-OK; `GNAM_D7_1` fires **27,001** decoys at pri 560 first), GObjects reaches truth only after `GOBJ_ES53_1`'s 21 decoys are validator-rejected. |
| Satisfactory (Satfi426 snapshot) | UE 4.26, **modular build** (Ghidra project `Satfi426.rep`) | **SUPERSEDED (2026-07-26) by `Satisfactory_UE426.rep` below — this project can be deleted.** **OFFLINE ONLY, and NOT usable for AOB mining as supplied.** The .rep contains only 3 game DLLs (`FactoryGame-GameAnalyticsModule`, `-CoffeeCore`, `-FactoryGame`) — the modules that DEFINE the globals (`FactoryGame-CoreUObject-Win64-Shipping.dll`, `-Engine-...dll`) were never imported. The game module does carry the IAT slots (`__imp_?GUObjectArray@@3VFUObjectArray@@A` `0x180722950`, `__imp_?GWorld@@3VUWorldProxy@@A` `0x180727CB8`, `__imp_?GEngine@@3PEAVUEngine@@EA` `0x180727CB0`) — which is exactly what a "via `_imp_`" pattern like `GOBJ_SF_1` resolves to, with `RipDeref` doing the second hop at runtime — but **every one of the 490 referencing sites is game code** (`UFG*`/`AFG*`/`FFG*`), so nothing mined there would generalise. The existing `SAT426` signatures were evidently derived from the CoreUObject/Engine modules; re-importing those two DLLs would make this project productive again. |
| Satisfactory v1.2.3.1 (current) | **UE 5.6.1**, modular (Ghidra project `Satisfactory_v1.2.3.1.rep` — CoreUObject + Core + Engine + FactoryGame all imported, **with PDBs**) | The UE5.6 band, and the modular layout done right (contrast the older `Satfi426.rep`, which lacks the defining modules). Ground truth is split across three DLLs: **CoreUObject** `GUObjectArray` `0x1805A3620` + `SparseDelegates` `0x1805661A0`; **Core** `NamePoolData` `0x18082E8C0` (via `FNameDebugVisualizer::GetBlocks` @ `0x18035CE20` = `lea rax,[0x18082E8D0]; ret`, minus 0x10 — note the pool moved from CoreUObject to **Core** by 5.6); **Engine** `GWorld` `0x18216DB68` + `GEngine` `0x182170748`. **All five targets already resolve with no new patterns** — GObjects `GOBJ_ES53_1`, GNames `GNAM_V5`, GWorld `GWLD_SF_1`, Sparse `SPARSE_ES2_1`, GEngine `GENG_X1` — each reaching truth (some after validator-rejected decoys). Kept as a regression binary for the UE5.6 band. |
| Drop In - VR Battle Royale (4.24 build) | **UE 4.24.3**, monolithic (exe `DropIn.exe`; Ghidra project `DropIn_UE424.rep`) | **OFFLINE ONLY** (needs SteamVR), full PDB (477,185 functions / 3,710,646 symbols) — an older build of the same title as the 4.27 row below, which makes it a second data point from one codebase. Ground truth: `GUObjectArray` `0x1471DB720`, `NamePoolData` `0x1471BCA00` (via `FNameDebugVisualizer::GetBlocks` @ `0x141323510` = `lea rax,[0x1471BCA10]; ret`, minus 0x10), `GWorld` `0x1472EA620`, `SparseDelegates` `0x146DA38D0`, `GEngine` `0x1472E74A0`. **Closed the last checkable sparse-delegate gap**: the mangled symbol demangles to `TMap<UObjectBase const*, TMap<FName, TSharedPtr<TMulticastScriptDelegate<FWeakObjectPtr>>>>` — a raw pointer key, identical to 4.25/4.26/4.27/5.x. Sparse delegates arrived in 4.23, so only 4.23 itself remains unverified. **Needs no new patterns** — all five targets resolve (`GOBJ_ES53_1` after 3 validator-rejected decoys, `GNAM_V8`, `GWLD_TQ_1`, `SPARSE_DI427_1`, `GENG_X1`). |
| Everspace 2 (2025-06-17 build) | **UE 5.5** (exe `ES2-Win64-Shipping.exe`; Ghidra project `ES2_UE55.rep`) | **OFFLINE ONLY**, full PDB (510,167 functions / 5,936,820 symbols). Two manifests newer than the `ES2-0517` snapshot — **the corpus's only same-game, same-engine cross-build pair**, and therefore the only thing that can test "does a signature survive a shipped patch?" as opposed to a version bump. Every global moved ~-0x2000 (`GUObjectArray` `0x149AA5F60`, `NamePoolData` `0x149BFE940`, `GWorld` `0x149B35DD8`, `SparseDelegates` `0x149AA5F10`, `GEngine` `0x149DA37B0`), so the patterns genuinely re-find them. **Both builds land on the same pattern with the same cost for all five targets** — `GOBJ_V13` (after 475 validator-rejected decoys from `GOBJ_ES53_1`), `GNAM_ES53_1`, `GWLD_ES2_1`, `SPARSE_ES2_1`, `GENG_X3`. |
| Meltopia | **UE 5.5**, monolithic (exe `Meltopia-Win64-Shipping.exe`, Steam; Ghidra project `Meltopia_V2.rep`) | **OFFLINE ONLY**, full PDB — a second symbolised **monolithic** UE 5.5 oracle, which the modular-heavy corpus was short of. ⚠ **The PDB only applies via the MSDIA loader — PDB-Universal fails on this file** (the first import, `Meltopia.rep`, silently produced zero UE globals and was usable only as a noise probe). Try MSDIA first when a game ships a PDB and the probe still finds nothing. Ground truth: `GUObjectArray` `0x149D87420`, `NamePoolData` `0x149CA3C80` (via `FNameDebugVisualizer::GetBlocks` @ `0x141270620`), `GWorld` `0x149F03D10`, `SparseDelegates` `0x149A9A070`, `GEngine` `0x149F002F8`. **Validated the pattern-consensus technique a second time** (after Everspace): while symbol-less, consensus predicted GEngine `149F002F8`, GWorld `149F03D10`, GObjects `149D87430` and GNames `149CA3C80` — all four matched the symbols exactly. Also the binary that flipped `GWLD_V7` from "0 correct anywhere" to UNIQUE-OK. |
| Palworld | **UE 5.1**, monolithic (exe `Palworld-Win64-Shipping.exe`; Ghidra project `Palworld.rep`) | **OFFLINE ONLY**, no symbols (322,040 functions) — the corpus's **only UE 5.1 sample**, used as a monolithic noise probe. It is the title `GOBJ_V13` / `GNAM_V8` / `GWLD_V7` / `GOBJ_V9` were contributed for, and all four fire on it. Consensus picks `GENG_X1` (3 hits), `GNAM_V8` → one pool agreed by 12 patterns, `GWLD_TQ_1` → one world agreed by 12, `SPARSE_ES2_1`. |
| Final Fantasy VII Rebirth (Ghidra) | **UE 4.26 fork**, monolithic (exe `ff7rebirth_.exe`; Ghidra project `FF7Re.rep`) | **OFFLINE ONLY**, no symbols (377,000 functions) — noise probe, and the **only binary that exercises `GOBJ_RE1` and the `GNAM_V7` CallFollow**, both contributed for this game. `GOBJ_RE1` had zero hits across the previous 31-program corpus and hits exactly once here: it was never broken, just never tested on its own title. (`GNAM_V7` is `AobResolve::CallFollow`, which the offline harness deliberately skips — it cannot follow the CALL and scan the callee, so it is scored in-game only.) |
| Satisfactory (UE 4.22 depot build) | **UE 4.22**, **monolithic** (exe `FactoryGame-Win64-Shipping.exe`, Steam depot re-download; Ghidra project `Satisfactory_UE422.rep`) | **OFFLINE ONLY**, full PDB (181,345 functions / 2,663,724 symbols) — and the only pre-4.25 **monolithic** oracle we have, which matters because modular Satisfactory DLLs are 4–30 MB of `.text` against a game EXE's 100–200 MB and badly understate pattern noise. Ground truth: `GUObjectArray` `0x144006F80`, `GWorld` `0x1441073B8`, `GEngine` `0x144104E58`, and the pre-FNamePool name array `Names` `0x144002A78` — a `TStaticIndirectArrayThreadSafeRead<FNameEntry,4194304,16384>*` lazily allocated in `FName::GetNames` @ `0x140BCEBF0`. No `SparseDelegates` (4.23+). **Exposed an unmatchable pattern:** `AOB_GNAMES_SAT422_1`, named after this very build, omitted the `48 85 C0` between the load and the jump and therefore got **zero hits on all 31 programs** in the sweep — MSVC cannot emit `mov`+`jnz` with no flag-setting instruction between them. Re-derived from this PDB and re-prioritised 730 → 715 (build 2408), so 4.22 now lands on `GNAM_SAT422_1` instead of falling through to `GNAM_CT4`. |
| Satisfactory (UE 4.26 depot build) | **UE 4.26.2**, modular (Ghidra project `Satisfactory_UE426.rep` — FactoryGame + Core + CoreUObject + Engine, **all with PDBs**) | **OFFLINE ONLY.** The 4.26 band done right; replaces the unusable `Satfi426.rep`. Ground truth is split across three DLLs (all at image base `0x180000000`, so `GS_TRUE` entries **must** be program-scoped — their ranges overlap): **CoreUObject** `GUObjectArray` `0x1803F9210` + `SparseDelegates` `0x1803F37D0`; **Core** `NamePoolData` `0x180659380`; **Engine** `GWorld` `0x18182A0B8` + `GEngine` `0x181826658`. **Confirms the sparse-delegate key shape at 4.26** — the PDB names the symbol `FSparseDelegateStorage::SparseDelegates`, closing another slice of the 4.23–4.26 gap left by the 4.25 and 4.27 oracles. Needs no new patterns: GObjects `GOBJ_ES53_1`, GNames `GNAM_DI427_2`, GWorld `GWLD_SF_2`, Sparse `SPARSE_ES2_1`, GEngine `GENG_X1` all reach truth. |
| Satisfactory (UE 5.2 depot build) | **UE 5.2.1**, modular (Ghidra project **`SF521_pdb.rep` — created by us**, see below) | **OFFLINE ONLY.** Closes the UE 5.2 hole. ⚠ The user-supplied `Satisfactory_UE521.rep` is **mis-imported**: only `FactoryGame-FactoryGame-Win64-Shipping.dll` is genuinely 5.2 (the *game* module, no engine globals); its `Core`/`CoreUObject`/`Engine` entries are **duplicates of the 4.26 DLLs** (their recorded `executablePath` points at `…\UE4.26.2\Engine\Binaries\Win64\` and their symbol counts match 4.26 exactly), plus four broken empty imports. The real DLLs + PDBs from `D:\tmp\Game archive\Satisfactory\UE5.2.1\Engine\Binaries\Win64\` were imported into a **separate** project so the original stays untouched — one at a time, since a Ghidra project takes an exclusive lock. Ground truth: **CoreUObject** `GUObjectArray` `0x1804194D0` + `SparseDelegates` `0x1803EDCB0`; **Core** `NamePoolData` `0x18073D0C0`; **Engine** `GWorld` `0x181CD4828` + `GEngine` `0x181CD1140` — each cross-checked against the DLL export table (GEngine RVA `0x1CD1140`, GWorld `0x1CD4828`, GUObjectArray `0x4194D0`). All five targets resolve with no new patterns. |
| Meltopia | **UE 5.5**, monolithic (exe `Meltopia-Win64-Shipping.exe`, Steam; Ghidra project `Meltopia.rep`) | **OFFLINE ONLY**, used as a monolithic UE 5.5 **noise probe** (165 MB exe, 331K functions). ⚠ It **does ship `Meltopia-Win64-Shipping.pdb` (347 MB)** next to the EXE, but the Ghidra import never applied it — `probe.java` finds zero UE globals, so it carries no ground truth. Re-importing it *with* the PDB would make it a second symbolised monolithic UE 5.5 oracle; not required, since `ES2-0517` already covers 5.5 with symbols. As a probe it behaves exactly like Manor Lords (the other monolithic 5.5): consensus picks `GENG_X1`/`GENG_X3`/`GENG_ES55_1` agreeing on one GEngine, `GNAM_V8` + 12 others on one name pool, and `GWLD_SP57_1` on one GWorld. |
| Everspace 2 (UE4.25 depot build) | **UE 4.25.2** (exe `ES2-Win64-Shipping.exe`, from a Steam depot re-download; Ghidra project `ES2-UE425.rep`) | **OFFLINE ONLY**, full PDB (192,091 functions / 1,967,871 symbols). The FField/FProperty transition band. Ground truth: `GUObjectArray` `0x1444B0510`, `NamePoolData` `0x144497D00` (via `FNameDebugVisualizer::GetBlocks` @ `0x140EF8410`), `GWorld` `0x1445F1160`, `SparseDelegates` `0x1440070C0`, `GEngine` `0x1445EDAD8`. **Closed the 4.23-4.26 sparse-delegate gap**: the PDB type is `TMap<UObjectBase const*, ...>` — a raw pointer key, identical to 4.27 and 5.x — so only 4.23/4.24 remain unverified. **Needs no new patterns**: GEngine `GENG_X1`, GNames `GNAM_V8`, GWorld `GWLD_TQ_1`, Sparse `SPARSE_DI427_1` all CORRECT-on-all-hits; GObjects reaches truth via `GOBJ_SAT425_2` after `GOBJ_ES53_1`'s 4 decoys are validator-rejected. Also extended two existing families: `SPARSE_DI427_1/_2` (mined on 4.27) are 3/3 correct here, and `GNAM_DI427_1/_2` likewise. |
| Drop In - VR Battle Royale | **UE4.27.2** (`++UE4+Release-4.27-CL-18319896`, built 2021-11-30; exe `DropIn.exe`, dir `Drop In - VR Battle Royale/DropIn/Binaries/Win64`, Steam) | **OFFLINE ONLY — never launched** (needs SteamVR). Included because it **ships its full 286 MB PDB**: the project's first symbolised UE 4.27 oracle, and the source of the `DI427` signatures + the `AobTarget::GEngine` target (build 2399). It is a **Development, non-editor** build (`.msvcjmc` + Live++ `.lpp_*` + `.uedbg` + engine source paths in `.rdata`), so treat its numbers as non-Shipping. Ghidra project `D:\Tools\GHIDRA_Projs\DropIn.rep`; ImageBase `0x140000000`, `.text` 129 MB, 657K functions / 5.38M symbols. Ground truth: `GUObjectArray` `0x14A3AA660` (→ ObjObjects, the tool's GObjects, `0x14A3AA670`), `NamePoolData` `0x14A363940` (recovered via `FNameDebugVisualizer::GetBlocks` = `lea rax,[0x14A363950]; ret`, minus `offsetof(Entries,Blocks)=0x10`), `GWorld` `0x14A52CED8` (`UWorldProxy`), `FSparseDelegateStorage::SparseDelegates` `0x149EC0910`, `GEngine` `0x14A528890`. **Replaying the whole signature database over its `.text` found GObjects 0/52 working** — the V-series pins the chunk-load destination register to `rcx`, which is the *index* register at all 400 access sites, and this build's `FUObjectItem` is **32 bytes** (StatID compiled in) so the stride math is `shl r,5`. `UObject::ProcessEvent` is at **vtable+0x220** (slot 68) — 4th independent 4.27 confirmation, first with a symbol proving it; `+0x218` is `UObject::OverridePerObjectConfigSection`. |
| The Artisan of Glimmith | UE4.27 (PE: 427, exe: Geri-Win64-Shipping) | Full working. Stride 24, 24K objects. GWorld ✅. **Build 648 cross-version PE-vtable verification target (2026-05-11)**: actual PE slot is `vtable+0x220` (old hardcoded `0x218` for UE 4.25-4.27 was off by 1 slot — enough to silently break invokes). Validator confirms 1260 hook fires / 1500ms. Four scenarios verified: Add_IntInt=7, Multiply_FloatFloat=12, `CharacterMovementComponent::GetMaxJumpHeight`=89.99 (instance method via game-thread dispatch), `PlayerCameraManager::GetCameraLocation` returns FVector struct. |
| Barn Finders | UE4.25 (PE: 425, exe: BarnFinders-Win64-Shipping) | User-submitted logs (build 560). Stride 24, 136,953 objects. UE5-Extended layout (strict). GWorld ✅. Standard FProperty mode. Publisher fallback (Atomic Jelly / Plug-In Digital — no thumbprint match). |
| Colossal | UE5.03 (PE: 503, exe: Colossal-Win64-Shipping, publisher: Atan) | User-submitted logs (build 560). Stride 24, 41,528 objects. UE5-Extended layout (strict). GWorld ✅. TaggedFFieldVariant (UE5.3+). FField::Next=+0x18, FField::Name=+0x20. Publisher Copyright is Epic default placeholder — no thumbprint match. |
| Extinction | UE4.15 (PE: 415, exe: Extinction.exe, dir: Blink/Binaries/Win64) | User-submitted logs (build 560). Stride 24, 230,732 objects. **Flat (non-chunked)** FFixedUObjectArray. GNames via TNameEntryArray (UE4Names=yes). UProperty mode (UE < 4.25). UField::Next=+0x28. **Not the lowest** — NEKOPALIVE (UE 4.11, row above) is, and `Grimoire::MIN_SUPPORTED_UE_VERSION = 411` matches it. This row claimed the floor until 2026-08-06, having been written when 4.15 really was the oldest thing tested. Patterns: GOBJ_RE2 (1.8s, 2 batches) / GNAM_CT3 (4.6s, 4 batches) / GWLD_G42_1 (3.3s, 3 batches). Publisher: Modus Games (no thumbprint). |
| MS Gundam SEED Battle Destiny Remastered (SBDR) | UE4.27 (PE: 427, exe: `SEED BATTLE DESTINY REMASTERED.exe`, dir: `Game_SBDR/Binaries/Win64`) | User-tested Steam title (build 1016). Stride 24, 57,423 objects (→72K mid-game). **All ✅** — GObjects (GOBJ_ES53_1, UE5-Extended layout strict, Max 2.16M / 33 chunks), GNames (GNAM_V8, FNamePool hdrOff=0 stride 2, UE4Names=no — a UE4.27 on the UE5-style FNamePool), **GWorld ✅** (GWLD_GH_1, `0x7FF758D77040`). FProperty mode (CPN=no, TagFFV=no, Outer=+0x20; FField Next=+0x20 Name=+0x28; FProp Offset=+0x4C StructProp=+0x78). `version.dll` proxy loads (real version.dll). ProcessEvent **vtable+0x220** — game-thread dispatch validated (15 646 hooks/1500 ms), invokes OK. Internal UClasses use a "Life" prefix (`LifeGameInstance`, 15 fields; `BP_LifeSaveData_C`). Bandai Namco (publisher=- — no thumbprint match). |
| The Adventures of Elliot: The Millennium Tales (冒險家艾略特的千年奇譚) | UE5.4 (runtime-reconciled — PE version stripped → publisher fallback 427, upgraded via tagged FFieldVariant→503 + CMC::GravityDirection→504, exe: `Elliot-Win64-Shipping.exe`, Steam folder `The Adventures of Elliot_The Millennium Tales/Elliot/Binaries/Win64`) | **The dxgi.dll proxy's motivating title** (build 1168+, 2026-06-15). **Re-verified on the FULL RELEASE (no longer the Prologue Demo), build 1363 (2026-06-19).** Stride 24, ~390K objects (389,292 at scan → 406,873 mid-game). UE5-Extended layout (strict; Max 2.16M / 33 chunks). **All ✅** — GObjects (GOBJ_ES53_1, `0x149BFB140`), GNames (GNAM_V8, `0x149B17600`, FNamePool hdrOff=0 stride 2, UE4Names=no — a UE4.27 on the UE5-style FNamePool like SBDR), **GWorld ✅** (GWLD_TQ_1, `0x149D8ADA0`). FProperty mode, TaggedFFieldVariant (UE5.3+), CPN=no, Outer=+0x20; FField Next=+0x18 Name=+0x20; FProp Offset=+0x44 StructProp=+0x70. Name sanity 10/10. **UE version: the stripped PE falls back to UE4.27, but runtime markers reconcile it upward to UE5.4** — tagged FFieldVariant → floor 5.3, then `CMC::GravityDirection` → 5.4 (self-correcting in `UE5_Init`; build ~1808, 2026-06-27). The UE5-style FNamePool + TaggedFFieldVariant were already UE5 tells; "4.27" was only ever the publisher-fallback guess. **ProcessEvent vtable+0x260 — game-thread dispatch validated (6 096 hooks / 1500 ms), invokes OK.** **The EXE imports NEITHER `version.dll` NOR `dinput8.dll` (only `dxgi` + `winmm`; D3D12 title)** → both existing proxies never load (confirmed by PE import-table analysis); the **`dxgi.dll` proxy DOES — LIVE-VERIFIED in-game** (`dxgi proxy: lazily forwarded 20/20 exports`, pipe + `trigger_scan` + `get_object_list` all OK). Internal UClasses use an `LS` prefix (`BPLSGameInstance_C`, `LSGameWork`, `CharacterAttributeSet`). Publisher SQUARE ENIX (no thumbprint match). |
| **Avowed** | UE5.3 (PE: 503) → **runtime-raised to 504** by the `CMC::GravityDirection` property marker — same mechanism as DragonSword Awakening, confirmed 2026-09-06 (exe: `Avowed-Win64-Shipping.exe`, dir: `Alabama/Binaries/Win64`, Obsidian, Steam) | **Solved end-to-end via Ghidra ground-truth (builds 1230-1240, 2026-06-17). LIVE-VERIFIED.** **The FIRST verified game with a PACKED 20-byte `FUObjectItem` (stride `0x14`, not the standard 24)** — Obsidian drops the padding (Object@0 / Flags@8 / ClusterRoot@0xC / Serial@0x10); reading at stride 24 misaligns every object → garbage names + a string-bytes "count" (`0x00750061`="ua"=7,667,809). ~213K objects (95K at main menu). **No standard GObjects AOB matches — not even patternsleuth's** (confirmed by running its CLI on the exe). GObjects is a **static `FUObjectArray` at RVA `0xB5BE398`**, found via NEW patterns **`GOBJ_AV1`** (`MOV RDX,[ObjObjects.Objects]` in `AllocateUObjectIndex`) + **`GOBJ_AV2`** (the generic FUObjectItem chunk-index codegen, ~10+ sites, patch-resilient; the `lea*5;shl<<2` bakes in the 20-byte stride) — both resolve to GObjects+0x10 (adjustment `-0x10`). UE5-Extended layout (strict; Num 213K, Max 2.16M / 33 chunks). GNames `GNAM_V5` ✅. **GWorld AOB lands on a decoy → recovered via `instance_scan_recovery`** ✅ — scans `.data` for a slot pointing to a non-CDO UWorld, then picks the ACTIVE game world by non-null `OwningGameInstance` (skips World-Partition `_Generated_` transient cells, which have higher index but no GameInstance). Injects via the **`dxgi.dll` proxy**. Tracking doc: [docs/avowed-gobjects-fix.md](avowed-gobjects-fix.md). |
| Echoes of Aincrad Demo (艾恩葛朗特 迴盪新聲 Demo) | UE5.4 (PE undetected → default 504, exe: `EchoesofAincrad-Win64-Shipping.exe`, dir: `EchoesofAincrad/Binaries/Win64`, Bandai Namco) | User-tested Steam demo (build 1257, 2026-06-17). Stride 24, 238,216 objects. UE5-Extended "Default" layout (strict). **All ✅** — GObjects (GOBJ_V13, `0x147FD0B00`), GNames (GNAM_V5, FNamePool hdrOff=0 stride 2, UE4Names=no), **GWorld ✅** (GWLD_G42_3, `0x148630048`; live world `PL_Dungeon`, World 38 / Level 29 fields, PersistentLevel found). FProperty mode, TaggedFFieldVariant (UE5.3+), CPN=no, Outer=+0x20; FField Next=+0x18 Name=+0x20; FProp Offset=+0x44 StructProp=+0x70. Offsets validated against real `Guid`/`Vector` structs (10/10 name sanity); enum names ✅ (`ENetRole`, UEnum::Names @ +0x40); Value Search + Locate-in-GWorld (FindObjectGraphPath, 2 hops) confirmed. PE doesn't stamp a UE version (`Product=1.0`) → defaulted to 504, confirmed correct by the struct validation. Injects via the **`dxgi.dll` proxy** (forwarded 20/20 exports — same D3D12 case as Elliot). Internal codename **ROD** (`BP_RODGameInstance_C` super `RODGameInstance`, 35 fields). A Sword Art Online title; publisher Bandai Namco (no thumbprint match). |
| Solarpunk (太陽龐克) | UE5.7 (PE: 507, exe: `SolarpunkSteam-Win64-Shipping.exe`, rokaplay GmbH; PE hash `ED3D085C0811F000`) | **The real-world repro that exposed + LIVE-CONFIRMED the UE5.7+ Object@+0x08 within-item detection (builds 1257-1259, 2026-06-17).** Stock UE5.7, **24-byte FUObjectItem with `Object`@+0x08** (`FlagsAndRefCount@0, Object@8, Serial@0x10`). 149,782 objects. **All ✅ after the fix** — the classic +0x00 pass is bad-dominated (`named=66 / bad=69`; a stride-16 scan over 24-byte items hits the real Object field only ~1/3 of the time) so it now falls through to the +0x08 pass → `FUObjectItem size=24, object-ptr offset=+0x08 — 200 named, 200 total, 0 bad` → name resolution **~100%** (`BP_MainPlayerController_C` 2 found, `nonNull==named`), sanity **10/10** (both were 45.9% / 4/10 *before* the fix). GObjects GOBJ_V13, GNames GNAM_SAT425_3 (after `GNAM_V5` hint miss → full scan), **GWorld ✅ via instance-scan recovery** (raw deref lands on a decoy `0x1C2D5` → recovered to the real UWorld). ProcessEvent vtable+0x260, dispatch validated (13 644 hooks/1500 ms). FProperty mode, TaggedFFieldVariant (UE5.3+); offset validation fell back to heuristic (Guid/Vector structs not found) but resolved correctly (ChildProps +0x50, FProp Offset +0x44). `version.dll` proxy. Closes the build-1064 "+0x08 still needs a real stock-5.7+ game" live-confirm. **Re-verified 2026-07-25 on an UPDATED binary (build 2380, PE hash now `ED3D085C0811F000`):** 149,782 objects, still all ✅ (item +0x08/24 `200 named / 0 bad`, UEnum::Names +0x40 UE5.6+ FNameData, sanity 10/10, `World size=2712`, walk log 0 WARN/0 ERROR / no unresolved FText); on this build GNames hits `GNAM_V5` directly and offsets resolve via struct 'Guid' (no heuristic fallback). **GWorld again via instance-scan recovery** — the EngineState carries `gworld_method=instance_scan_recovery` with `gworld_aob` NULLED, so the Live Walker "AOB" export toggle is correctly disabled (the canonical fallback-GWorld case behind the AOB-toggle persistence exception, dev `2a497c4`). Publisher rokaplay (no thumbprint). **AOB path repaired (build 2383, 2026-07-25, PDB-mined):** the recovery masked a real AOB failure — all pri-10-20 UE5 GWorld patterns get 0 hits on this UE5.7 build, so `GWLD_SF_2` (pri 21) matched a decoy `.data` global `0x1478C25A8` (0xA8B0 below the real GWorld `0x1478CCE58` = runtime `0x7FF655F1CE58`) that `UE5_Init`'s deref-guard caught → recovery. Added `GWLD_SP57_1..4` (pri 100–160 after the build-2393 priority re-spread; were 10–13) + `SPARSE_SP57_1/2` (`SPARSE_ES2_1` was 0/21), each verified decoy-free against the real `.text` via the new `tools/ghidra/dump_global_xref_aob.java` + `verify_aob.java`. **LIVE-VERIFIED (build 2384):** GWorld flipped `instance_scan_recovery` → **`aob`** — `GWLD_SP57_1` unique-matched the real GWorld `0x7FF6D4D1CE58` (scan 143 ms vs 1.24 s), and `SPARSE_SP57_1` found SparseDelegates `0x7FF6D4B1A4B0` (was `not_found`); `FindAll: Complete` all four `(aob)`. Also hardened `ValidateGWorldBasic` with a vtable code-pointer decoy guard (build 2387). **⚠ M5 crash found:** enabling See-through then closing the game crashes VERSION.dll (`0xc0000409`, event-log Id 1000) — tracked as a separate follow-up. See [dev-log 2026-07-25](dev-log.md). |
| Stellar Blade (劍星) | UE4.26 fork (detected UE426, exe: `SB-Win64-Shipping.exe`, dir: `StellarBlade/SB/Binaries/Win64`, Shift Up, Steam) | User-tested (build 1928, 2026-07-05). Stride 24, 264,332 objects. **UE5-Extended layout (strict)** despite the UE4.26 fork (like SBDR / FF7 Rebirth — a UE4 on UE5-style structures; stride-24 wins 200/200, Object@+0x00). **All ✅** — GObjects (`0x146DD6230`), GNames (`0x146FFDC00`, FNamePool hdrOff=0 stride 2, Format A shift 6, UE4Names=no — a UE5-style pool on a UE4.26 fork), **GWorld ✅** (`0x146FFA8F8`). Name sanity 10/10. FProperty mode (UE 426 ≥ 425), CPN=no, TagFFV=no, Outer=+0x20; UStruct Super=+0x40 Children=+0x48 ChildProps=+0x50 PropsSize=+0x58; FField Next=+0x20 Name=+0x28; FProp Offset=+0x4C ElemSize=+0x3C StructProp=+0x78. `version.dll` proxy (loads the real version.dll from system32). **Fly (Dunste) verified** — view-relative velocity-drive flight works (MOVE_Flying held, velocity sticks). Publisher Shift Up (no thumbprint match). |
| Persona 3 Reload (女神異聞錄３ Reload) | UE4.27 fork (PE VERSIONINFO stripped `Product=1.0` → tool defaults to **504**, detected=no; **ProcessEvent slot vtable+0x220 = the UE4.25-4.27 slot** like SBDR/Geri; UE5-Extended layout + TaggedFFieldVariant. exe `P3R.exe`, dir `P3R/P3R/Binaries/Win64`, ATLUS/SEGA, Steam) | User-tested (build 1948, 2026-07-06, log dir `P3R`). Stride 24 (`Object`@+0x00; 200/200 named, 0 bad — stride-16 bad=69, stride-20 bad=104), 65,165 objects at scan (Max 2.16M / 33 chunks). **UE5-Extended layout (strict)** + **TaggedFFieldVariant (UE5.3+)**, FProperty mode, CPN=no — a UE4.27 title on UE5-style structures (like SBDR / Stellar Blade). **All globals ✅** — GObjects (GOBJ_ES53_1, 129 matches, `0x1457B4D10`), GNames (GNAM_V8 CallFollow, `0x1457789C0`, FNamePool hdrOff=0 stride 2, Format A shift 6, BlockOffsetBits 16, UE4Names=no, FName[0]='None'), **GWorld ✅ directly** (GWLD_GH_1 unique, `0x1458F8870` — no decoy/recovery), SparseDelegates (SPARSE_ES2_1, `0x145422370`). Offsets validated against real `Guid`/`Vector` structs: Outer=+0x20; UStruct Super/Children/ChildProps/PropsSize/Script = 0x40/0x48/0x50/0x58/0x60; FField Class/Next/Name = 0x08/0x20/0x28; FProp ElementSize/Offset = 0x3C/0x4C. **GWorld walk ✅** — World 35 fields (size 1944), GameInstance `GlobalWork` (super GameInstance, 43 fields, size 199216), PlayerController `ItfController` (super `KernelInput`, 133 fields), LocalPlayer 9 fields; WalkFunctions 32. **⚠ Foreground-pause quirk (`t.IdleWhenNotForeground=1`):** the game idles its game thread whenever it is NOT the foreground window, so the ProcessEvent hook-fire validator failed (**0 fires / 1500 ms**) and invoke/POV getters time out → POV falls back to the raw cached-POV read while backgrounded. The +0x220 slot is likely correct (pattern-matched, version-independent) — it just couldn't self-validate with the thread asleep. **Fix — Teleport tab → Keep Foreground (Grausam), LIVE-VERIFIED build 1955 (2026-07-06):** P3R's pause is **WM_ACTIVATEAPP-message-driven, not GetForegroundWindow polling** (the config `t.IdleWhenNotForeground 0` AND a GetForegroundWindow-only hook both had no effect). Grausam now hooks GetForegroundWindow **and** subclasses the game window's WndProc, rewriting the deactivation messages to "active" (WM_ACTIVATEAPP/WM_ACTIVATE/WM_NCACTIVATE→active, swallow WM_KILLFOCUS). Log confirms it subclassed the real 3840×2160 game window; invoke/POV ops (POV/Teleport/Console/Interesting-Functions) then work while backgrounded. `version.dll` proxy (loads the real system32 version.dll). PE hash `69CE343916376000`. Publisher ATLUS/SEGA (`(C)ATLUS. (C)SEGA.`, no thumbprint match). |
| Pionero Capital Demo | UE5.7 (PE: 507, exe: `PioneroCapital-Win64-Shipping.exe`, dir: `PioneroCapital/Binaries/Win64`, Pionero Games, Steam) | User-tested Steam demo (build 1.0.0.2202, 2026-07-15, log dir `PioneroCapital-Win64-Shipping`). **The second stock-UE5.7 `Object`@+0x08 title after Solarpunk** — a 24-byte `FUObjectItem` with the object pointer at +0x08 (`item_layout_mode=unpacked57`; `FlagsAndRefCount@0, Object@8, Serial@0x10`), 117,663 objects (Max 2.16M / 2 live chunks / 33 max). The classic +0x00 pass is bad-dominated (`named=66 / bad=69` — a stride-16 read over 24-byte items) so `Aura::DetectItemSize` falls through to the +0x08 pass → `FUObjectItem size=24, object-ptr offset=+0x08 (UE5.7+ reordered item) — 200 named, 200 total, 0 bad` → name sanity **10/10**. **All ✅** — GObjects (GOBJ_ES53_1, 89 matches, UE5-Extended layout strict, `0x7FF6F65689D0`), GNames (GNAM_V5, 11 matches, `0x7FF6F649A940`, FNamePool hdrOff=0 stride 2, Format A shift 6, BlockOffsetBits 16, UE4Names=no, FName[0]='None'), **GWorld ✅ directly** (GWLD_TQ_1 unique match, `0x7FF6F66F3780` — **no decoy / instance-scan recovery**, unlike Solarpunk), SparseDelegates (SPARSE_ES2_1, `0x7FF6F6296FF0`). FProperty mode, **TaggedFFieldVariant (UE5.3+)**, CPN=no (votes standard=20 / CPN=0), Outer=+0x20; UStruct Super/Children/ChildProps/PropsSize/Script = 0x40/0x48/0x50/0x58/0x60; FField Class/Next/Name = 0x08/0x18/0x20; FProp ElemSize/Flags/Offset = 0x34/0x38/0x44; FStructProp::Struct +0x70. Offsets validated against real `Guid`/`Vector` structs; enum names ✅ (`ENetRole`, UEnum::Names @ +0x40, UE5.6+ FNameData). FArrayProperty::Inner self-corrected +0x70 → +0x78. **GWorld walk ✅** — live world `NewPioneroIsland` (World, 42 fields, propsSize 2712), PersistentLevel @+48 (Level, 29 fields), GameInstance `JoebillGameInstance` (super GameInstance, 6 fields, size 456), LocalPlayer 9 fields, WorldSettings 143 fields, GameStateBase 88 fields; WalkFunctions OK. Value Search (Int32/Float/Double) + a NumericNoByte snapshot exercised clean (0 warnings/errors across all 5 log files). Loads via the **`dxgi.dll` proxy** (`load_mode=proxy:dxgi.dll`, forwarded 20/20 exports to the real System32 dxgi — a D3D12 title like Elliot / Echoes of Aincrad / Avowed; **the first stock-UE5.7 title verified end-to-end through the dxgi proxy**). Internal UClasses use a **`Joebill`** prefix (`BP_JoebillGameMode_C` 99 fields, `BP_JoebillPlayer_C` : `JoebillPlayer` 184 fields, `BP_MH_JoebillCharacter_C` 252 fields) — a city-builder / tycoon sim (`CitygenPrimitiveSpawnSettings`, `BP_TrafficLightsController_C`, `BP_PedTraffic_C`, `CC_Customizable_Character_C`). PE hash `C200F9770A5F1000`. Publisher Pionero Games (Copyright is the Epic default placeholder `Fill out your copyright notice…` — no thumbprint match). |
| Star Trek Voyager – Across the Unknown | UE5.6 (PE: 506, exe: `STVoyagerSteam-Win64-Shipping.exe`, dir: `STVoyager/Binaries/Win64`, Steam) | User-tested 2026-07-23 (build 1.0.0.2360, log dir `STVoyagerSteam-Win64-Shipping`, PE hash `4720D6A80ABFA000`). **Stock UE5.6, everything resolved on the first try — no fork handling, no fallback, no recovery path.** 171,074 objects (Max 2,162,688 / **3 live chunks** / 33 max), UE5-Extended `Default` layout (strict, preset 1/5), **stride 24** — the sweep is unambiguous here (24: `200 good / 200 named / 0 bad`; 16: `67 good / 66 null / 67 bad`; 20: `34 good / 65 null / 101 bad`). **All three globals ✅** — GObjects `0x7FF60F8085A0`, GNames `0x7FF60FA20EC0`, **GWorld `0x7FF60FC8F628` directly** (no decoy, no instance-scan recovery). FNamePool hdrOff=0 stride 2, chunks at `+0x10` (validated with `None`), `FName[0]='None'`; name sanity **10/10**. FProperty mode, **TaggedFFieldVariant (UE5.3+)**, CPN=no (votes standard=20 / CPN=0), Outer=+0x20; UStruct Super/Children/ChildProps/PropsSize = 0x40/0x48/0x50/0x58; FField Next=+0x18 Name=+0x20; FProp ElemSize=+0x34 Offset=+0x44 StructProp=+0x70 — all stock UE5. **GWorld walk ✅** — navigated GWorld → `PersistentLevel` (+0x30) → `OwningWorld` (+0xC0) → `OwningGameInstance` (+0x228) in the Live Walker. Also exercised clean: `walk_instance` ×25, `walk_functions` ×25, **Value Search** (5 sessions), a **NumericNoByte snapshot**, and `walk_world`. Loads via the **`dxgi.dll` proxy** (forwarded 20/20 exports to the real System32 dxgi) — a D3D12 title like Elliot / Echoes of Aincrad / Pionero Capital. The UI resolved 152,519 of 171,074 names (89.2%). |
| **NEKOPALIVE** (ネコパラ) | **UE4.11** (PE: 411, detected — the OLDEST engine this tool supports; 4.10 and below are gated off outright, see below. exe: `Nekopara.exe`, dir `NEKOPALIVE/Package/Nekopara/Binaries/Win64`, Steam) | User-tested 2026-07-27 (build 1.0.0.2481, log dir `Nekopara`, PE hash `56FDD99A026C9000`). **The floor of the supported range, and the title that forced three separate fixes no newer game could expose.** 29,799 objects (Max 2,097,152). **FLAT `FFixedUObjectArray` presented at the `FUObjectArray` BASE**, not at `ObjObjects` — the new **`Flat-Base`** preset (`Objects@+0x10 / Max@+0x18 / Num@+0x1C`); log reads `preset Flat-Base, Num=29799, Max=2097152, Objects=0x209101A0000 [flat]`. `FUObjectItem` is **16 BYTES** here (`Object` / **`ClusterAndFlags`** / `SerialNumber` — cluster index and flags share one int32; read off Epic's source at `4.11.0-release`, 4.13 splits them and grows to 24). **All ✅** — GObjects (`GOBJ_GH_4` unique, `0x7FF743911510`), GNames (`GNAM_CT3`, `0x2090DFDFAF0` — a **`TNameEntryArray`**, heap-allocated: pre-4.23 has no `FNamePool` at all; `strOff=0x10, entry[0]='None', entry[1]='BytePro'`), **GWorld ✅ directly via `GWLD_FD_1`** (`0x7FF7439F4370`; 18 patterns tried, **only FD_1 hit**), **&GEngine ✅** (`GENG_X1`, `0x7FF7439F2DE0`, resolved through the deferred path — `FindAll` reports `GEngine=0x0 (deferred)` then it lands once `ValidateAndFixOffsets` has run). **SparseDelegates `not_found` is the CORRECT answer** — sparse delegates were introduced in UE 4.23. **UProperty mode** (411 < 425), CPN=no (votes standard=20 / CPN=0), Outer=+0x20; UStruct Super/Children/ChildProps/PropsSize = 0x30/0x38/0x50/0x40; UProperty Next=+0x28 Name=UObject+0x18 Offset=+0x50 ElemSize=+0x34; validated against a real `Guid` struct. Name sanity **10/10**. **GWorld walk ✅** — World (26 fields, size 2368) → Level (13) → WorldSettings (108) → `NekoparaGameInstance_C` (5) → LocalPlayer (8) → `NekoparaPlayerController_C` (1552 bytes). Loads via the **`version.dll` proxy**. **Three fixes it forced, each invisible on any newer title:** (1) the **`Flat-Base`** preset — the old flat preset was written `ObjObjects`-RELATIVE, which held only because 5 of ~56 GObjects patterns carry `adjustment = -0x10`; when those miss, every hitting pattern resolves the BASE and the game was **unfixable by pattern work at ANY priority**. (2) The flat precheck was **count-gated** (only ran above 65,536 objects), so this title's 29,799 fell through to the CHUNKED probe, which reads `Item[0].Object` as a chunk pointer — 1 usable item in 200 and **~10% names**; believing the preset took it to **100%**. Fantasynth (4.13, identical layout, 80,162 objects) was perfect the whole time — *the only difference was the object count*. (3) `FindPropertyOffsetByName` walked only the **FField** chain, so on EVERY pre-4.25 title it read a non-existent member and returned -1, silently disabling `ValidateGEngineSlot`, `FindLiveGameEngine` and `RecoverGWorldViaEngine` — i.e. **no GEngine AOB could ever validate on 4.11–4.24**. **Steam discovery:** its layout is `<Game>/Package/<Sub>/Binaries/Win64/Nekopara.exe` — an extra wrapper level AND no `-Win64-Shipping` suffix — which broke Scan Steam until it grew a bounded depth-3 search where **shallowest depth wins**. |

| **Gal\*Gun: Double Peace** | **Unreal Engine 3** (x64) — **REFUSED BY DESIGN, LIVE-VERIFIED build 2516** (PE hash `57FC3FE20352D000`; `scan-0.log`: 4/4 markers → `sentinel 300` → `SKIPPING the scan`, marker sweep ~31 ms, zero AOB passes). PE `ProductVersion` is a GAME version (`1.0.10897.0`), exe `GG2Game.exe`, dir `GalGun Double Peace/Binaries/Win64`, Inti Creates / PQube, Steam | **Not a supported title — the entry exists to document the refusal and the measurements behind it.** Ships wxWidgets 2.8, PhysX 2.8 (`PhysXLoader64`/`NxCharacter64`), APEX legacy, msvcr100 (VS2010), `UnrealDebugger.dll` + `EasyHook64.dll`, and a mixed-mode CLR image (`mscoree!_CorExeMain`, populated COR20 header). NOT packed — entry RVA `0x1E9621E` is inside `.text`, `.reloc` intact; the odd `.nep` section is 151 × 16-byte `EB 08 / 0F 0B / jmp [rip]` dispatch stubs (an EasyHook/nFringe pad), **not** a packer. **Why the pre-existing 4.11 floor did not catch it:** `DetectVersionFromPEResource` returns 0 (major is 1), and Tier 1/2/3 of the memory scan get **zero hits** — the needle table floors at `"4.18."`, so nothing can match a UE3 version. `CompanyName` is `Epic Games, Inc.` and `kPublishers` holds only `SQUARE ENIX`, so control reached the detection-failed fallback: **`UEVersion = 504`, `bLowConfidence = true`, `bVersionDetected = false`** — three of the gate's four conjuncts failing, the first (`504 < 411`) decisively. It therefore paid a full 150-pattern sweep to reach "no winner" and then advised a UE-version override that could never work. Now caught by `CountPreUE4Markers` → `Grimoire::PRE_UE4_SENTINEL_VERSION = 300` → the existing gate; **scores 4/4 markers** (`UnrealEngine3` 3 ASCII / 1 UTF-16, `SeqAct_` 10 / 108, `PhysXLoader64` 1, Epic copyright `1998-2012`). **65 supported binaries score 0/4** (30 reference builds UE 4.10–5.8 + all 35 installed UE games), re-measurable with `py tools/pe/pre_ue4_markers.py --corpus`. **Why UE3 was not implemented:** measured from its own exported `UObject::GetOutermost` (this build ships nFringe UnrealScript-debugger hooks, so the accessor is name-resolvable — a free ground-truth anchor that will NOT generalise), `Outer` is @+0x40 and `Class` @+0x50, versus `OFF_UOBJECT_CLASS = 0x10` here — so `ValidateCyclicClassChain`, which gates every `ValidateGObjects` accept path, rejects even a **correct** UE3 GObjects address. AOB patterns *do* land on the real `UObject::GObjObjects` (`TArray<UObject*>`, confirmed by the decorated symbol string in its `/clr` name pool AND by disassembly), and `Aura::ArrayLayout` could already express it as `{0x00, 0x0C, 0x08, -1, -1}` — the blocker is the hardcoded validators, not the layout. See [technical-notes.md](technical-notes.md) §"Pre-UE4". **PROXY, if anyone ever injects it anyway: use `dinput8.dll`.** It imports `DINPUT8.dll` (only `DirectInput8Create`) and `WINMM.dll` (16 fns) statically, `dxgi.dll`/`d3d11.dll` only as DELAY imports, and **no `version.dll` at all**. `dinput8` is preferred over `winmm` for two measured reasons: (1) `Lugner_Winmm.cpp` sets `s_done = true` *before* its `LoadLibraryW`, so one transient failure permanently leaves `mProcs[]` null and the asm thunk jumps through zero, whereas the dinput8 C forwarder returns `E_FAIL`; (2) `wxmsw28u_adv_vc_custom_64.dll` — a static import of the exe — itself imports WINMM, so its `DllMain` runs under the loader lock, which is the Octopath crash shape documented at `Lugner_Dxgi.cpp:26`. |

-----

### MindsEye — working on game v7.3.1 (GObjects ✅ / GNames ✅ de-obfuscated; game-specific symbols renamed at build time)

| Game | UE Version | Notes |
|------|-----------|-------|
| **MindsEye** | UE5.4.4 licensee fork (PE: 504, exe: `MindsEye-Win64-Shipping.exe`, dir: `MindsEye/Binaries/Win64`, Build A Rocket Boy, Steam; `version.dll` proxy) | **Solved end-to-end (builds 2220 + 2238), LIVE-VERIFIED on GAME VERSION 7.3.1 ONLY (PE hash `0863E3B90C993000`) — every RVA/offset below is build-specific; full re-derivation playbook in [mindseye-fork-notes.md](mindseye-fork-notes.md).** `GUObjectArray` RVA `0x0BB139B0` via new **`MindsEye-Extended`** preset (MaxElements `+0x10`, NumChunks `+0x14`, MaxChunks `+0x20`, NumElements `+0x24`, Objects `+0x28`) plus a **preset-bound `itemHint`**: `FUObjectItem` is **32 bytes with `UObject*` at `+0x10`** (65536 elem/chunk). 530,638 objects, Max 2,162,688 / 9 live chunks / 33 max, `200 total / 0 bad`. The item layout MUST stay preset-bound — 32/`+0x10` aliases perfectly with stride 16 and would steal TQ2 / Octopath if added to the shared sweep. GWorld ✅ (`GWLD_ES2_6` unique, `0x7FF60EE239B8`), SparseDelegates ✅ (`SPARSE_ES2_1`). Binary is **unpacked, no Denuvo/EAC/BattlEye**; `.rdata` keeps the `__FILE__` anchors (`J:\work\e18f6e32b612e2cd\Engine\Source\Runtime\...`), which is how the layout was recovered offline (capstone + `.pdata`, no Ghidra). ~206 MB exe / 145 MB `.text` from statically-linked Wwise 2023.1 + NVIDIA NGX + MetaHuman + the BARB `Everywhere`/`Arcadia` platform. **GNames ✅ — pool RVA `0x0BA306C0`, payload de-obfuscated (build 2238, experimental-gated).** Entry = stock `u16 header` at `+0x00` (`len = header>>6`), a **non-stock `u16 tag` at `+0x02`**, then XOR'd chars at **`+0x04`** (stock: `+0x02`), 2-byte aligned. Key is **per TAG** (an early per-block hypothesis was wrong), looked up in the fork's own open-hash map: tag `0x0001`→`0x09`, `0x0002`/`0x0082`→`0x5B`, `0x0003`→`0x1D`, `0x0016`→`0xA3`, `0x0036`→`0xE3`, `0x0061`→`0xC9`. Block 0 decodes to the canonical `None`, `ByteProperty`, `IntProperty`, … EName list. Decrypt routine RVA `0x0178B440` (ANSI) / `0x0178B540` (wide); `KeyDerive` RVA `0x0178CF50` is a locked hash-map probe (`bucket = tag & (capacity-1)`) into a runtime table at ctx RVA `0x0BA47700`. We **read that table directly and never call the routine** (its lock is taken before the probe, so a fault inside it would leave `FName::ToString` wedged forever). ctx is located statically: `Sig::AOB_NAMEDECRYPT_ME1` -> `E8 rel32` at match+`0x2F` -> getter -> first rip-relative `48 8D 05`. Live: `decrypt fn=0x7FF60492B440 getter=0x7FF604931050 keyTable=0x7FF60EBE7700`; name sanity **10/10**. **Game-specific symbols are UNRECOVERABLE by design** — MindsEye renamed its own non-engine property/class names to generated 16-char all-lowercase identifiers at build time (`wcxugjojsqaqvers`, ...); those strings sit verbatim in `.rdata` and the exe holds 21,635 distinct such tokens, so the originals exist nowhere. Engine symbols read normally. The pak/IoStore AES (CUE4Parse `MindsEyeAes.cs`) is real but irrelevant to in-process scanning. PE hash `0863E3B90C993000`. |

-----

## Untested / analysis-predicted candidates

> Games we have NOT run the dumper against, but for which we can predict coverage
> from static AOB analysis. **No live confirmation** — treat as leads, not results.

### Halo Campaign Evolved (UE 5.5) — GObjects predicted via `GOBJ_V11`, NOT yet tested

RE-UE4SS added a per-game config for this title (upstream commit `4b96f82b`,
2026-07-25). It is **stock UE 5.5, standard layout** (no packed/reordered item, no
licensee fork) — the only reason RE-UE4SS ships a custom config is a game-specific
GUObjectArray AOB, not a non-standard structure. Their signature (from
`assets/CustomGameConfigs/Halo Campaign Evolved/UE4SS_Signatures/GUObjectArray.lua`):

```
45 84 C0                test r8b, r8b
48 C7 41 10 00000000    mov  [rcx+0x10], 0
48 8D 05 ?? ?? ?? ??    lea  rax, [rip+GObjects]   ← anchor (match offset 0x0B)
4C 8B C9                mov  r9, rcx
48 89 01                mov  [rcx], rax
B8 FF FF FF FF          mov  eax, -1
89 41 08                mov  [rcx+8], eax
```

**Prediction: our existing `GOBJ_V11` already matches this** (byte-for-byte over the
tail). `GOBJ_V11` = `48 8D ?? ?? ?? ?? ?? 4C 8B C9 48 89 01 B8 FF FF FF FF` (Himmel.h,
priority 14, "Little Nightmares 3"): its 2-byte `48 8D` + 5 wildcards absorb Halo's
`48 8D 05` + disp32, then `4C 8B C9 48 89 01 B8 FF FF FF FF` matches literally →
resolves the same `lea rax,[rip+GObjects]`. `GOBJ_RE3` (LN3 Demo, priority 13) is the
same idiom extended with `89 41 08 0F 45 …` and would also match if Halo's tail
continues with the `cmovne`. **Halo uses the identical `FUObjectArray`-init idiom as
Little Nightmares 3** — so we most likely resolve its GObjects with zero new patterns.
Not added to the tested table (analysis only; no game copy to confirm).

#### ⚠ Multiple-match risk — pre-diagnosed so a future fix is fast

`GOBJ_V11` is **short and generic**: `lea reg,[rip]; mov r9,rcx; mov [rcx],rax;
mov eax,-1` is a common shape, so on some binaries it can match **more than one
site**. RE-UE4SS ships the *longer* Halo pattern (with the `test r8b,r8b; mov
[rcx+0x10],0` prefix) precisely to be unambiguous. So there is a real, if small,
chance that on Halo `GOBJ_V11`/`GOBJ_RE3` resolve to a **wrong** `lea` and we pick a
bad GObjects.

**What already protects us** (why this usually self-corrects, no action needed):
- **Priority ordering** — `GOBJ_V11` is priority 14, tried early; the first
  *validating* match wins.
- **`ValidateGObjects`** — the resolved target must look like a chunked/flat
  `FUObjectArray` (plausible `Num`/`Max`/chunk-table); a wrong `lea` target fails
  validation and the scan moves on. This is the same guard that rejects the many
  `ValidateGObjects: Failed` candidates seen in every scan log.

**Symptoms that it went wrong on Halo** (what to look for in the logs): GObjects at a
non-`.data`/non-module address, an implausible object count, `named ≈ bad` at every
stride, sanity < 10/10, or `Start from GWorld` empty despite a live world.

**Paste-ready fix if it does** — add the longer, unique RE-UE4SS anchor to
`dll/src/Himmel.h` at a **higher** priority than `GOBJ_V11` (lower number = higher
precedence). It is safe to rank high because the `45 84 C0 48 C7 41 10 00000000`
prefix is specific enough that it will not steal LN3 or other `GOBJ_V11` games:

```cpp
// In the AOB definitions:
constexpr const char* AOB_GOBJECTS_HALO_1 =
    "45 84 C0 48 C7 41 10 00 00 00 00 48 8D 05 ?? ?? ?? ?? 4C 8B C9 48 89 01 B8 FF FF FF FF 89 41 08";

// In GOBJECTS_PATTERNS[] (priority ~11, above GOBJ_RE3=13 / GOBJ_V11=14):
//   instrOffset=0x0B (the lea sits 11 bytes into the match — RE-UE4SS's OnMatchFound
//   uses MatchAddress+0xB), ripOffsetInInstr=3, instrLen=7, adjustment=0.
SIG_RIP("GOBJ_HALO_1", AOB_GOBJECTS_HALO_1, AobTarget::GObjects, 0x0B, 3, 7, 0, 11,
        "HALO", "Halo Campaign Evolved UE5.5 FUObjectArray init (unique prefix)"),
```

Source: RE-UE4SS `assets/CustomGameConfigs/Halo Campaign Evolved/UE4SS_Signatures/GUObjectArray.lua` — its `OnMatchFound` resolves `MatchAddress + 0xB` as the `lea`, confirming the `0x0B` instruction offset.

-----

## GWorld Status Summary

**Working (35/35):** NEKOPALIVE (UE4.11, `GWLD_FD_1` direct — the oldest engine in the list), TQ2, EverSpace 2, Hogwarts Legacy, IDOLM@STER, Romancing SaGa 2, Tower of Mask, Ghostwire: Tokyo, Cat Island Petrichor Demo, Way of the Hunter 2 Demo, COMBAT PILOT Demo, OctoPath Traveler, FF7R, FF7Re, DQ I&II, DQ III, DQ XI S, Lushfoil Photography Sim, Manor Lords, The Artisan of Glimmith, Barn Finders, Colossal, Extinction, MS Gundam SEED Battle Destiny Remastered, The Adventures of Elliot: The Millennium Tales, Avowed (instance_scan_recovery), Echoes of Aincrad Demo, Star Wars Jedi: Fallen Order (CE manual inject), Satisfactory (modular CoreUObject DLL — v1.1.3.1/UE5.3 + v1.2.3.1/UE5.6), Solarpunk (instance-scan recovery), Stellar Blade, Persona 3 Reload (GWLD_GH_1 direct), Pionero Capital Demo (GWLD_TQ_1 direct, stock UE5.7 Object@+0x08, dxgi proxy), Star Trek Voyager – Across the Unknown (stock UE5.6, all three globals direct), MindsEye (UE5.4.4 licensee fork, **game v7.3.1 only** — see [mindseye-fork-notes.md](mindseye-fork-notes.md))

**Failing (GWorld not found or untested):** none — all 34 tested games resolve GWorld. Star Wars Jedi + Satisfactory were re-verified working at build 704 (2026-05-12); the earlier "failing" note was stale (pre-`AOBScanAllModules` / pre-CE-inject). Satisfactory was re-verified again on its **1.2.3.1 / UE5.6** build (build 1.0.0.1718, 2026-06-25) — symbol-export globals + GWorld walk all clean with zero code changes.

## Naming Convention

- **NEKOPALIVE** = NEKOPALIVE / ネコパラ (UE4.11, Steam; executable `Nekopara.exe`, project dir
  `NEKOPALIVE/Package/Nekopara/Binaries/Win64/` — note the **extra `Package/<Sub>` wrapper level and the
  missing `-Win64-Shipping` suffix**, which is what broke Scan Steam; internal UClasses use a `Nekopara`
  prefix — `NekoparaGameInstance_C`, `NekoparaPlayerController_C`). **The oldest supported engine (4.11) and
  the verified floor of the whole project** — below it, 4.10 replaces `FFixedUObjectArray` with
  `TStaticIndirectArrayThreadSafeRead`, has **no `FUObjectItem` at all**, and uses an INLINE chunk table that
  `ArrayLayout` structurally cannot express (`objectsOffset` means "read a pointer here"; 4.10 needs "take the
  ADDRESS of here"). That is why <4.11 is gated as unsupported rather than left to fail confusingly.
- **FF7Re** = Final Fantasy VII Rebirth (UE4.26 Square Enix fork)
- **FF7R** = Final Fantasy VII Remake Intergrade (UE4.18 Square Enix fork)
- **Geri** = The Artisan of Glimmith (UE4.27, executable `Geri-Win64-Shipping.exe` — log folder uses this name)
- **Blink** = Extinction (UE4.15, executable `Extinction.exe`, parent dir `Blink/Binaries/Win64/`)
- **SBDR** = MOBILE SUIT GUNDAM SEED Battle Destiny Remastered (UE4.27, Steam; executable `SEED BATTLE DESTINY REMASTERED.exe`, project dir `Game_SBDR/Binaries/Win64/`; internal UClasses use a `Life` prefix — `LifeGameInstance`, `BP_LifeSaveData_C`, etc.)
- **Elliot** = The Adventures of Elliot: The Millennium Tales / 冒險家艾略特的千年奇譚 (UE5.4 — its stripped PE detects as UE4.27 via publisher fallback, runtime-reconciled upward to UE5.4 via tagged FFieldVariant + CMC::GravityDirection markers; SQUARE ENIX, Steam; executable `Elliot-Win64-Shipping.exe`, Steam folder `The Adventures of Elliot_The Millennium Tales/Elliot/Binaries/Win64/`). Now the **full release** (re-verified build 1363, 2026-06-19; originally tested as the Prologue Demo). The title that motivated the **`dxgi.dll` proxy** (3rd proxy type) — its EXE imports neither `version.dll` nor `dinput8.dll`, only `dxgi`/`winmm`. Internal UClasses use an `LS` prefix.
- **TQ2** = **Titan Quest II** (Steam; installed at `…\steamapps\common\Titan Quest II\TQ2\Binaries\Win64\`).
  Recorded 2026-08-20 because the abbreviation appears **18 times** in the pending-verification
  register (it is GROUP 8's whole title) and was **not expandable anywhere in `docs/`** — a
  session trying to schedule GROUP 8 could not tell what to launch.
- **Avowed** = Avowed (UE5.3, Obsidian, Steam; executable `Avowed-Win64-Shipping.exe`, project dir `Alabama/Binaries/Win64/`). The title that proved **packed 20-byte `FUObjectItem`** support and the **static-struct GObjects AOBs** (`GOBJ_AV1`/`GOBJ_AV2`) for games where no standard/patternsleuth pattern matches. Injects via `dxgi.dll` proxy.
- **Echoes of Aincrad** = Echoes of Aincrad Demo / 艾恩葛朗特 迴盪新聲 Demo (UE5.4, Bandai Namco, Steam; executable `EchoesofAincrad-Win64-Shipping.exe`, Steam folder `Echoes of Aincrad Demo/EchoesofAincrad/Binaries/Win64/`; internal UClasses use a `ROD` prefix — `RODGameInstance`, `BP_RODGameInstance_C`). A Sword Art Online title. PE stamps no UE version (`Product=1.0`) → defaults to 504, confirmed UE5.3+/5.4 by struct validation. Injects via the `dxgi.dll` proxy (3rd proxy type, like Elliot — D3D12 EXE imports neither `version.dll` nor `dinput8.dll`).
- **P3R** = Persona 3 Reload / 女神異聞錄３ Reload (UE4.27 fork, ATLUS/SEGA, Steam; executable `P3R.exe`, project dir `P3R/P3R/Binaries/Win64/`; internal UClasses: GameInstance `GlobalWork`, PlayerController `ItfController` : `KernelInput`). PE version stripped (`Product=1.0`) → tool defaults to 504; UE5-Extended + TaggedFFieldVariant layout with a UE4.25-4.27 ProcessEvent slot (**vtable+0x220**, like SBDR/Geri). All globals + GWorld walk ✅. Notable for the **foreground-pause quirk** (`t.IdleWhenNotForeground=1`): the game thread idles when the window is not focused, so invoke/POV getters time out (ProcessEvent validator saw 0 fires) while backgrounded — keep the game focused or set `t.IdleWhenNotForeground 0`. `version.dll` proxy.
- **Solarpunk** = Solarpunk / 太陽龐克 (stock UE5.7, rokaplay GmbH, Steam; executable `SolarpunkSteam-Win64-Shipping.exe`). The real-world repro that exposed + live-confirmed the **UE5.7+ `Object`@+0x08** within-item layout (24-byte item): its classic +0x00 pass is bad-dominated (`named ≈ bad`, a mis-strided read), so `Aura::DetectItemSize` now falls through to the +0x08 pass (the `bestNamed > bestBad` quality gate, build 1257). `version.dll` proxy.
- **Pionero Capital Demo** = Pionero Capital Demo (stock UE5.7, Pionero Games, Steam; executable `PioneroCapital-Win64-Shipping.exe`, project dir `PioneroCapital/Binaries/Win64/`; internal UClasses use a `Joebill` prefix — `JoebillGameInstance`, `BP_JoebillGameMode_C`, `BP_JoebillPlayer_C`; a city-builder / tycoon sim). No Chinese title — English only. The **second stock-UE5.7 `Object`@+0x08 title after Solarpunk**, re-confirming the +0x08 fall-through on a different game, but with two firsts: **GWorld resolves directly** (GWLD_TQ_1 unique — no decoy / instance-scan recovery that Solarpunk needed) and it is the **first stock-UE5.7 title verified through the `dxgi.dll` proxy** (D3D12 EXE, like Elliot / Echoes of Aincrad / Avowed). Build 1.0.0.2202, 2026-07-15.
